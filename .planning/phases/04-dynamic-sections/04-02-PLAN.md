---
phase: 04-dynamic-sections
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified: [src/app/actions/condense.ts, src/components/editor/CustomSections.tsx, src/components/ResumeBuilder.tsx, src/components/preview/PreviewPane.tsx]
autonomous: true
requirements: [AI-CONDENSE, DYN-UI]
must_haves:
  truths:
    - "AI can successfully condense a full resume to fit one A4 page"
    - "Users can add, remove, and edit arbitrary resume sections in the LHS editor"
    - "Users can preview AI-condensed drafts before applying them"
  artifacts:
    - path: "src/app/actions/condense.ts"
      provides: "Gemini-powered condensation logic"
    - path: "src/components/editor/CustomSections.tsx"
      provides: "Dynamic form UI for arbitrary sections"
  key_links:
    - from: "src/components/ResumeBuilder.tsx"
      to: "src/app/actions/condense.ts"
      via: "server action call"
    - from: "src/components/ResumeBuilder.tsx"
      to: "src/components/editor/CustomSections.tsx"
      via: "component rendering"
---

<objective>
Implement the core "Condense to 1-Page" AI workflow and the dynamic form UI for arbitrary resume sections. This allows users to manage complex resumes and leverage AI to hit the strict 1-page target.
</objective>

<execution_context>
@C:/Users/kshir/.gemini/get-shit-done/workflows/execute-plan.md
@C:/Users/kshir/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-dynamic-sections/04-RESEARCH.md
@.planning/phases/04-dynamic-sections/04-01-SUMMARY.md
@src/store/useResumeStore.ts
@src/components/ResumeBuilder.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement AI Condense Server Action</name>
  <files>src/app/actions/condense.ts</files>
  <behavior>
    - Accepts ResumeData (original).
    - Uses gemini-3-flash to prune content while maintaining relevance.
    - Returns a valid ResumeData object (draft).
    - Follows the selective pruning rules: detailed recent roles, reduced older roles, limited bullets.
  </behavior>
  <action>
    Create `src/app/actions/condense.ts`. Use Gemini 3 Flash and the Vercel AI SDK.
    The prompt should be very specific about the 1-page A4 target and the JSON structure.
    Ensure it handles `customSections` by condensing their content too.
  </action>
  <verify>
    <automated># Manual verification via UI with a long resume</automated>
  </verify>
  <done>Server action correctly condenses resume data into a 1-page format.</done>
</task>

<task type="auto">
  <name>Task 2: Build Dynamic Custom Sections Editor UI</name>
  <files>src/components/editor/CustomSections.tsx</files>
  <action>
    Create `src/components/editor/CustomSections.tsx` using `react-hook-form` and `useFieldArray`.
    1. Define a local form for the custom sections.
    2. Use `useFieldArray` for the top-level sections (e.g., 'Certifications').
    3. Use nested `useFieldArray` for items within each section.
    4. Provide UI to add/remove sections and add/remove items.
    5. Sync the local form state with the Zustand store (`original` state) on every change or using a debounced effect.
    6. Ensure Biome is used for formatting.
  </action>
  <verify>
    <automated># Manual check: Can add/remove/edit custom sections in UI</automated>
  </verify>
  <done>UI supports adding and editing an arbitrary number of custom resume sections.</done>
</task>

<task type="auto">
  <name>Task 3: Integrate Condense Flow into ResumeBuilder</name>
  <files>src/components/ResumeBuilder.tsx, src/components/preview/PreviewPane.tsx</files>
  <action>
    1. Update `ResumeBuilder.tsx`:
       - Add a "Condense to 1-Page" button in the header.
       - Trigger `condenseResume` action and call `setDraft` in the store.
       - If `draft` is present in the store:
         - Show a "Previewing Draft" banner.
         - Show "Apply Changes" and "Discard Draft" buttons.
         - `Apply` calls `applyDraft()`.
         - `Discard` calls `discardDraft()`.
       - Render the `CustomSections` component in the editor section.
    2. Update `PreviewPane.tsx` (or the rendering logic in `ResumeBuilder`):
       - Pass `draft || original` to the preview components so the user sees the condensed version during preview.
  </action>
  <verify>
    <automated># Manual check: Full flow from Condense -> Preview -> Apply/Discard works</automated>
  </verify>
  <done>User can trigger condensation, preview the result, and choose to apply or discard it.</done>
</task>

</tasks>

<verification>
- Test adding a "Certifications" section with 2 items and verify it appears in the preview.
- Test the "Condense" button with a 2-page resume and verify the AI reduces it to 1 page.
- Verify that "Apply Changes" updates the master data and "Discard" reverts to the original.
</verification>

<success_criteria>
- AI condensation is reliable and preserves high-impact information.
- Dynamic sections allow for any resume content type.
- Preview/Commit flow provides a safe user experience for AI enhancements.
</success_criteria>

<output>
After completion, create `.planning/phases/04-dynamic-sections/04-02-SUMMARY.md`
</output>
