---
phase: 04-dynamic-sections
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/schemas/resume.ts, src/store/useResumeStore.ts, src/app/actions/extract.ts]
autonomous: true
requirements: [DYN-SCHEMA, DRAFT-STATE]
must_haves:
  truths:
    - "Resume schema supports arbitrary custom sections"
    - "Store handles Master (original) and Draft (condensed) versions"
    - "PDF extraction captures non-standard sections into customSections"
  artifacts:
    - path: "src/schemas/resume.ts"
      provides: "Updated Zod schema with customSections"
    - path: "src/store/useResumeStore.ts"
      provides: "Dual-state storage logic with Immer"
    - path: "src/app/actions/extract.ts"
      provides: "Enhanced AI extraction for custom sections"
  key_links:
    - from: "src/store/useResumeStore.ts"
      to: "src/schemas/resume.ts"
      via: "importing ResumeData and custom types"
---

<objective>
Upgrade the data architecture to support dynamic sections and implement the "Master vs Draft" state management. This provides the foundation for AI-driven condensation without destructive edits to the user's primary data.
</objective>

<execution_context>
@C:/Users/kshir/.gemini/get-shit-done/workflows/execute-plan.md
@C:/Users/kshir/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-dynamic-sections/04-RESEARCH.md
@src/schemas/resume.ts
@src/store/useResumeStore.ts
@src/app/actions/extract.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Upgrade Zod Schema with Custom Sections</name>
  <files>src/schemas/resume.ts</files>
  <behavior>
    - CustomSectionItemSchema should have id, title, subtitle, date, and description (string array).
    - CustomSectionSchema should have id, title (e.g., 'Certifications'), and items (array of items).
    - ResumeSchema should include customSections: z.array(CustomSectionSchema).default([]).
    - DEFAULT_RESUME should be updated with empty customSections.
  </behavior>
  <action>
    Modify `src/schemas/resume.ts` to include the new schemas and update `ResumeSchema` and `DEFAULT_RESUME`.
    Ensure all fields that can be empty are handled correctly with `.optional().or(z.literal(""))`.
    Export types for `CustomSection` and `CustomSectionItem`.
  </action>
  <verify>
    <automated>npx tsx -e "import { ResumeSchema } from './src/schemas/resume'; ResumeSchema.parse({ personalInfo: { fullName: 'Test' }, customSections: [{ id: '1', title: 'Certifications', items: [] }] })"</automated>
  </verify>
  <done>Zod schema supports dynamic custom sections and types are exported.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Master/Draft State in Zustand with Immer</name>
  <files>src/store/useResumeStore.ts</files>
  <action>
    Refactor `src/store/useResumeStore.ts`:
    1. Install immer: `npm install immer`.
    2. Update `ResumeState` interface:
       - Rename `data` to `original` (the source of truth).
       - Add `draft: ResumeData | null` (for AI-condensed versions).
       - Add `setOriginal(data: ResumeData)`.
       - Add `setDraft(data: ResumeData)`.
       - Add `applyDraft()`: Sets `original = draft` and `draft = null`.
       - Add `discardDraft()`: Sets `draft = null`.
    3. Use `immer` middleware or the `set((state) => produce(state, draft => ...))` pattern for all update methods to simplify nested updates (especially for customSections).
    4. Implement `add/update/removeCustomSection` and `add/update/removeCustomSectionItem` methods.
    5. Update existing methods (addExperience, etc.) to target `state.original`.
  </action>
  <verify>
    <automated>npm test src/store/useResumeStore.ts (if tests exist) or verify by running the app and checking state in devtools</automated>
  </verify>
  <done>Store supports dual-state management and dynamic section updates using Immer.</done>
</task>

<task type="auto">
  <name>Task 3: Update PDF Extraction to Capture Custom Sections</name>
  <files>src/app/actions/extract.ts</files>
  <action>
    Modify `src/app/actions/extract.ts`:
    1. Update internal `ExtractionResumeSchema` to include `customSections`.
    2. Define `ExtractionCustomSectionItemSchema` and `ExtractionCustomSectionSchema` within the file (matching the structure in Task 1 but without IDs).
    3. Update the Gemini `system` prompt:
       - "Extract the information from the provided PDF resume accurately into the structured format."
       - "Identify sections that do not fit into Experience, Education, Skills, or Projects (e.g., Certifications, Awards, Volunteering, Languages, Publications) and map them to the `customSections` array."
       - "Each custom section should have a clear title reflecting its content."
    4. In the result processing, ensure IDs are generated for all extracted items (experience, education, skills, projects, AND customSections) before returning to the client.
  </action>
  <verify>
    <automated># Manual verification via UI with a complex resume PDF</automated>
  </verify>
  <done>AI extraction pipeline captures arbitrary sections and maps them to the new schema.</done>
</task>

</tasks>

<verification>
- Validate that the Zod schema correctly parses a resume with multiple custom sections.
- Verify that Zustand store methods correctly update the `original` state and handle the `draft` transition.
- Test PDF extraction with a resume containing "Certifications" or "Languages" to see if they appear in `customSections`.
</verification>

<success_criteria>
- Schema is flexible enough for any additional resume sections.
- State management allows for "Previewing" AI changes before committing them.
- AI extraction is more comprehensive, capturing sections previously ignored.
</success_criteria>

<output>
After completion, create `.planning/phases/04-dynamic-sections/04-01-SUMMARY.md`
</output>
