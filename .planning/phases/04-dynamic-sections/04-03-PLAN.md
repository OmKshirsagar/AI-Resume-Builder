---
phase: 04-dynamic-sections
plan: 03
type: execute
wave: 3
depends_on: ["04-01", "04-02"]
files_modified: [package.json, src/mastra/index.ts, src/mastra/agents/index.ts, .env.example, src/env.js]
autonomous: true
requirements: [MASTRA-INIT]
user_setup:
  - service: postgres
    why: "Mastra workflow durability and snapshots"
    env_vars:
      - name: DATABASE_URL
        source: "Local or hosted PostgreSQL connection string (e.g., postgresql://user:pass@localhost:5432/db)"
---

<objective>
Initialize Mastra in the project, configure it to use Gemini 3 Flash as the primary LLM, and set up the foundation for durable agentic workflows.
</objective>

<execution_context>
@C:/Users/kshir/.gemini/get-shit-done/workflows/execute-plan.md
@C:/Users/kshir/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-dynamic-sections/04-MASTRA-RESEARCH.md
@package.json
@src/env.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install Mastra Dependencies and Setup Environment</name>
  <files>package.json, .env.example, src/env.js</files>
  <action>
    1. Install core Mastra packages: `npm install @mastra/core @mastra/ai-sdk @mastra/pg`.
    2. Add `DATABASE_URL` to `.env.example`.
    3. Update `src/env.js` to include `DATABASE_URL` in the server schema and runtimeEnv.
    4. Ensure `GOOGLE_GENERATIVE_AI_API_KEY` is present in the environment (it should be used by Mastra's Google provider).
  </action>
  <verify>
    <automated>npm list @mastra/core && npx tsx -e "import { env } from './src/env'; console.log('Env validated')"</automated>
  </verify>
  <done>Mastra dependencies installed and environment variables configured.</done>
</task>

<task type="auto">
  <name>Task 2: Configure Base Mastra Instance</name>
  <files>src/mastra/index.ts</files>
  <action>
    Create `src/mastra/index.ts`:
    1. Import `Mastra` from `@mastra/core`.
    2. Import `PostgresStore` from `@mastra/pg`.
    3. Initialize `Mastra` with a `storage` instance using `DATABASE_URL` from `env.DATABASE_URL`.
    4. Define an empty `workflows` and `agents` object for now.
    5. Export the `mastra` instance.
    Note: If DATABASE_URL is not available in development, provide a fallback or instructions for the user.
  </action>
  <verify>
    <automated>npx tsx -e "import { mastra } from './src/mastra'; console.log('Mastra instance created')"</automated>
  </verify>
  <done>Base Mastra instance is initialized with PostgreSQL storage.</done>
</task>

<task type="auto">
  <name>Task 3: Define Base LLM Configuration and Agent Structure</name>
  <files>src/mastra/agents/index.ts</files>
  <action>
    Create `src/mastra/agents/index.ts`:
    1. Define specialized agents for the fabrication workflow (placeholders with basic instructions):
       - `auditorAgent`: Optimized for analysis and critique. "Review the provided resume against standard ATS practices."
       - `architectAgent`: Optimized for planning and structure. "Create a blueprint for a high-impact 1-page resume."
       - `fabricatorAgent`: Optimized for content generation and formatting. "Rewrite resume content according to the provided blueprint."
    2. Use `google/gemini-3-flash` as the model for all agents.
    3. Export these agents for use in workflows.
    4. Update `src/mastra/index.ts` to include these agents in the `Mastra` configuration.
  </action>
  <verify>
    <automated>npx tsx -e "import { mastra } from './src/mastra'; console.log('Agents configured:', Object.keys(mastra.agents))"</automated>
  </verify>
  <done>Agents using Gemini 3 Flash are defined and registered with Mastra.</done>
</task>

</tasks>

<verification>
- Verify Mastra can be imported and initialized without errors.
- Ensure `DATABASE_URL` is correctly picked up by the environment validator.
</verification>

<success_criteria>
- `src/mastra/index.ts` exists and exports a valid `mastra` instance.
- Specialized agents are defined using `gemini-3-flash`.
- Project is ready for workflow implementation.
</success_criteria>

<output>
After completion, create `.planning/phases/04-dynamic-sections/04-03-SUMMARY.md`
</output>
