---
phase: 04-dynamic-sections
plan: 04
type: execute
wave: 4
depends_on: ["04-03"]
files_modified: [src/mastra/workflows/fabricator.ts, src/app/api/workflow/fabricate/route.ts, src/components/ResumeBuilder.tsx]
autonomous: true
requirements: [MASTRA-FABRICATE]
must_haves:
  truths:
    - "Audit-Architect-Fabricate workflow correctly processes a full resume"
    - "Workflow progress is streamed to the UI in real-time"
    - "Final draft from workflow is valid and can be applied/discarded"
  artifacts:
    - path: "src/mastra/workflows/fabricator.ts"
      provides: "The 3-step AI fabrication workflow"
    - path: "src/app/api/workflow/fabricate/route.ts"
      provides: "Streaming API endpoint for Mastra"
  key_links:
    - from: "src/components/ResumeBuilder.tsx"
      to: "/api/workflow/fabricate"
      via: "fetch/useCompletion call"
    - from: "/api/workflow/fabricate"
      to: "src/mastra/workflows/fabricator.ts"
      via: "handleWorkflowStream call"
---

<objective>
Implement the "Resume Fabrication Workflow" using Mastra Workflows. This includes the Audit, Architect, and Fabricate steps, and integrating the streaming progress into the UI for a transparent AI experience.
</objective>

<execution_context>
@C:/Users/kshir/.gemini/get-shit-done/workflows/execute-plan.md
@C:/Users/kshir/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-dynamic-sections/04-MASTRA-RESEARCH.md
@.planning/phases/04-dynamic-sections/04-03-SUMMARY.md
@src/schemas/resume.ts
@src/mastra/index.ts
@src/mastra/agents/index.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement Workflow Steps (Audit, Architect, Fabricate)</name>
  <files>src/mastra/workflows/fabricator.ts</files>
  <behavior>
    - Audit: Analyzes ResumeData and optional Job Description. Returns findings (array of gaps/opportunities).
    - Architect: Takes Audit findings. Returns a "Blueprint" (structured plan for condensation/optimization).
    - Fabricate: Takes the Blueprint and ResumeData. Produces a refined/condensed ResumeData object.
    - Every step must use Zod schemas for input/output validation.
    - Every step must use `writer.write` to push progress events (e.g., { type: 'progress', payload: { message: 'Auditing resume...' } }).
  </behavior>
  <action>
    Create `src/mastra/workflows/fabricator.ts`:
    1. Define `auditStep`, `architectStep`, and `fabricateStep` using `createStep`.
    2. Use `ResumeSchema` (from `src/schemas/resume.ts`) for validation where applicable.
    3. Implement the `fabricatorWorkflow` using `createWorkflow` and `.step().then()` chaining.
    4. Register the workflow in `src/mastra/index.ts`.
  </action>
  <verify>
    <automated>npx tsx -e "import { mastra } from './src/mastra'; console.log('Workflow loaded:', !!mastra.workflows.fabricatorWorkflow)"</automated>
  </verify>
  <done>Full multi-step fabrication workflow is implemented and validated.</done>
</task>

<task type="auto">
  <name>Task 2: Create Streaming API Route for Fabrication</name>
  <files>src/app/api/workflow/fabricate/route.ts</files>
  <action>
    Create `src/app/api/workflow/fabricate/route.ts`:
    1. Use `handleWorkflowStream` from `@mastra/ai-sdk`.
    2. Hook up the `fabricatorWorkflow`.
    3. Accept `resumeData` and `jobDescription` in the POST body.
    4. Use `createUIMessageStreamResponse` from `ai` to return an NDJSON stream that handles Mastra events.
  </action>
  <verify>
    <automated># Manual verification via curl: Verify NDJSON stream returns progress events and final result</automated>
  </verify>
  <done>API endpoint exists that streams workflow progress and the final result.</done>
</task>

<task type="auto">
  <name>Task 3: Integrate Mastra Workflow into ResumeBuilder UI</name>
  <files>src/components/ResumeBuilder.tsx</files>
  <action>
    1. Update the "Condense to 1-Page" trigger in `ResumeBuilder.tsx` to call `/api/workflow/fabricate`.
    2. Implement a stream consumer that parses NDJSON events.
    3. Show step-by-step progress status (Audit -> Architect -> Fabricate) based on the streamed events.
    4. On completion, the stream should provide the final draft `ResumeData`. Call `setDraft` with this data.
    5. Ensure the UI gracefully handles errors and potential workflow timeouts.
  </action>
  <verify>
    <automated># Manual check: Triggering condensation shows step-by-step progress and produces a valid draft</automated>
  </verify>
  <done>UI provides transparent feedback during the multi-step AI optimization process.</done>
</task>

</tasks>

<verification>
- Test the full workflow with a complex resume.
- Verify that progress messages appear in the UI as the AI works through the steps.
- Ensure the final result is a valid `ResumeData` object that can be applied to the master state.
</verification>

<success_criteria>
- Workflow reliably executes all three steps.
- Progress visualization enhances user trust during long-running tasks.
- Final generated resume adheres to the 1-page A4 target.
</success_criteria>

<output>
After completion, create `.planning/phases/04-dynamic-sections/04-04-SUMMARY.md`
</output>
