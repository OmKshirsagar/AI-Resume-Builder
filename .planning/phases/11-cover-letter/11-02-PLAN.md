---
phase: 11-cover-letter
plan: 02
type: execute
wave: 2
depends_on: [11-01]
files_modified: [src/app/editor/[id]/cover-letter/page.tsx, src/components/cover-letter/DraftingLab.tsx, src/components/export/CoverLetterPDFDocument.tsx]
autonomous: true
requirements: [AI-ENHANCEMENT, RESUME-MANAGEMENT]
---

<objective>
Build the Cover Letter Drafting Lab UI and implement PDF export functionality.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Create Cover Letter Dynamic Route</name>
  <files>src/app/editor/[id]/cover-letter/page.tsx</files>
  <action>
    - Implement the dynamic route `/editor/[id]/cover-letter`.
    - Fetch the parent Resume data to provide context for the drafting lab.
    - Layout should include a navigation link back to the main editor.
  </action>
  <verify>Navigate to /editor/[some-id]/cover-letter and see the drafting page load.</verify>
  <done>User can access the cover letter interface for any resume.</done>
</task>

<task type="auto">
  <name>Task 2: Build the Drafting Lab UI</name>
  <files>src/components/cover-letter/DraftingLab.tsx</files>
  <action>
    - Implement a split-pane interface:
      - Left: Form for JD input, Company Name, Tone dropdown, and Length slider.
      - Right: Live preview of the generated markdown (using a markdown renderer).
    - Add a "Download PDF" button that triggers the export flow.
  </action>
  <verify>Enter a JD, select tone, and confirm the letter generates and displays correctly.</verify>
  <done>Interactive drafting experience is functional.</done>
</task>

<task type="auto">
  <name>Task 3: Implement Cover Letter PDF Export</name>
  <files>src/components/export/CoverLetterPDFDocument.tsx</files>
  <action>
    - Create a @react-pdf document for Cover Letters.
    - Re-use the header styles (Name, Contact Info) from the ResumeRenderer for branding consistency.
    - Handle markdown-to-pdf conversion for the letter body.
  </action>
  <verify>Download a generated cover letter and confirm it matches the resume's visual style.</verify>
  <done>Professional PDF output is ready for users.</done>
</task>

</tasks>

<success_criteria>
- Users can generate letters specific to a resume version.
- Drafting Lab supports real-time parameter updates (Tone/Length).
- PDF export is visually consistent with the main resume templates.
</success_criteria>

<output>
After completion, create `.planning/phases/11-cover-letter/11-02-SUMMARY.md`
</output>
