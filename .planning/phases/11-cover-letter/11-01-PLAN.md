---
phase: 11-cover-letter
plan: 01
type: execute
wave: 1
depends_on: [10-02]
files_modified: [src/db/schema.ts, src/mastra/workflows/cover-letter.ts, src/app/actions/cover-letter.ts]
autonomous: true
requirements: [AI-ENHANCEMENT]
---

<objective>
Implement the database schema, multi-step Mastra workflow, and server actions for AI Cover Letter synthesis.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Update Database Schema</name>
  <files>src/db/schema.ts</files>
  <action>
    - Add `coverLetters` table with fields:
      - `id`: text (PK)
      - `userId`: text (FK -> users.id)
      - `resumeId`: text (FK -> resumes.id)
      - `jobDescription`: text
      - `content`: text (generated markdown)
      - `companyName`: text
      - `tone`: text
      - `length`: text
      - `createdAt/updatedAt`: timestamps
    - Define relations between Resumes and CoverLetters.
  </action>
  <verify>Run `npm run db:push` (or check locally) to ensure schema is valid.</verify>
  <done>Persistence layer for cover letters is ready.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Mastra Synthesis Workflow</name>
  <files>src/mastra/workflows/cover-letter.ts</files>
  <action>
    - Create a 3-step workflow:
      - `analyze-jd`: Extract requirements and company culture from JD.
      - `match-experience`: Select the most relevant points from ResumeData.
      - `write-draft`: Synthesize the letter using the selected points and user parameters (tone/length).
    - Use `google/gemma-3-27b-it` or `gemini-3-flash-preview` for specific steps.
  </action>
  <verify>Manually trigger the workflow with test data and log the output draft.</verify>
  <done>AI engine can generate tailored letters.</done>
</task>

<task type="auto">
  <name>Task 3: Create Cover Letter Server Actions</name>
  <files>src/app/actions/cover-letter.ts</files>
  <action>
    - Implement `generateCoverLetter(resumeId, jd, params)`: Runs the Mastra workflow and streams/returns the draft.
    - Implement `saveCoverLetter(id, data)`: Persists the generated content to the DB.
    - Implement `listCoverLetters(resumeId)`: Fetches letters linked to a specific resume version.
  </action>
  <verify>Confirm actions can call the workflow and write to the database.</verify>
  <done>Backend logic is fully integrated.</done>
</task>

</tasks>

<success_criteria>
- New `cover_letters` table exists and is linked correctly.
- Mastra workflow produces high-quality, grounded drafts.
- Server actions successfully bridge the UI and the AI engine.
</success_criteria>

<output>
After completion, create `.planning/phases/11-cover-letter/11-01-SUMMARY.md`
</output>
