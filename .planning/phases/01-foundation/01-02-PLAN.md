---
phase: 01-foundation
plan: 02
type: execute
wave: 1
depends_on: ["01-01"]
files_modified:
  - src/store/useResumeStore.ts
  - src/hooks/use-store.ts
  - src/schemas/resume.ts
autonomous: true
requirements:
  - PHASE1-STATE
  - PHASE1-PERSIST
must_haves:
  truths:
    - "Resume state is managed by Zustand 5 with manual hydration to prevent SSR mismatch."
    - "The state is validated by Zod schemas."
    - "The UI does not attempt to render persisted state until the client has hydrated."
  artifacts:
    - path: "src/store/useResumeStore.ts"
      provides: "Zustand store with persistence"
    - path: "src/hooks/use-store.ts"
      provides: "SSR-safe hydration hook"
---

<objective>
Implement the core resume data store using Zustand 5, Zod validation, and a safe hydration pattern to ensure compatibility with Next.js SSR.

Purpose: Robust state management with data integrity.
Output: Validated, persistent, and SSR-safe data store.
</objective>

<execution_context>
@C:/Users/kshir/.gemini/get-shit-done/workflows/execute-plan.md
@C:/Users/kshir/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define Resume Zod Schemas & Types</name>
  <files>src/schemas/resume.ts</files>
  <action>
    - Create `src/schemas/resume.ts` and define the structure for a resume.
    - Include sub-schemas for: `PersonalInformation`, `Experience`, `Education`, `Skill`, `Project`.
    - Export a unified `ResumeData` type inferred from the Zod schema.
    - Add default values for a blank resume state.
  </action>
  <verify>
    <automated>npx tsc src/schemas/resume.ts --noEmit</automated>
  </verify>
  <done>Zod schemas and TypeScript types defined for the entire resume structure.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Zustand 5 Store with Safe Hydration</name>
  <files>src/store/useResumeStore.ts, src/hooks/use-store.ts</files>
  <action>
    - Create `src/store/useResumeStore.ts` using Zustand 5's `create` and `persist` middleware.
    - Implement actions for updating each section (e.g., `updatePersonalInfo`, `addExperience`, etc.).
    - Create `src/hooks/use-store.ts` using the SSR-safe hydration pattern from research (uses `useEffect` to guard state access).
    - This pattern prevents the common "Hydration failed" error when using `persist` middleware in Next.js.
  </action>
  <verify>
    <automated>grep -q "persist" src/store/useResumeStore.ts && grep -q "useEffect" src/hooks/use-store.ts</automated>
  </verify>
  <done>Zustand store implemented with persistence and an SSR-safe access hook.</done>
</task>

</tasks>

<verification>
Verify store updates in dev tools, check localStorage, and ensure page refreshes don't cause hydration errors in the console.
</verification>

<success_criteria>
- Resume state persists in localStorage.
- No "Hydration failed" warnings in the console.
- Typescript inference works for all store actions.
</success_criteria>

<output>
After completion, update `.planning/phases/01-foundation/01-02-SUMMARY.md`
</output>
