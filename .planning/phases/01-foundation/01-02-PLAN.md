---
phase: 01-foundation
plan: 02
type: execute
wave: 1
depends_on: ["01-01"]
files_modified:
  - src/store/useResumeStore.ts
  - src/schemas/resume.ts
autonomous: true
requirements:
  - PHASE1-STATE
  - PHASE1-PERSIST
must_haves:
  truths:
    - "Resume state is managed by Zustand."
    - "The state is validated by Zod."
    - "Resume state persists in localStorage."
  artifacts:
    - path: "src/store/useResumeStore.ts"
      provides: "Global state store"
    - path: "src/schemas/resume.ts"
      provides: "Zod validation schemas"
---

<objective>
Implement global resume state management using Zustand and Zod, with localStorage persistence.

Purpose: Provides a single source of truth for resume data.
Output: Zustand store for resume data with persistence and validation.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Resume Schema Definition</name>
  <files>src/schemas/resume.ts</files>
  <action>
    - Define `ResumeSchema` using Zod.
    - Include sections for: Personal Info, Experience, Education, Skills, and Projects.
    - Export TypeScript types inferred from the schemas.
  </action>
  <done>Zod schemas and types defined.</done>
</task>

<task type="auto">
  <name>Task 2: Zustand Store Implementation</name>
  <files>src/store/useResumeStore.ts</files>
  <action>
    - Create a Zustand store using `useResumeStore`.
    - Implement `persist` middleware for `localStorage`.
    - Add actions to update individual sections of the resume.
    - Implement a reset action.
  </action>
  <done>Zustand store with persistence and update actions implemented.</done>
</task>

</tasks>

<verification>
Verify that changes in the store are reflected in `localStorage` and persist across page refreshes.
</verification>

<success_criteria>
- Resume data can be updated via Zustand actions.
- Data persists in `localStorage` automatically.
- Refreshing the page preserves the resume state.
</success_criteria>

<output>
After completion, update `.planning/phases/01-foundation/01-02-SUMMARY.md`
</output>
