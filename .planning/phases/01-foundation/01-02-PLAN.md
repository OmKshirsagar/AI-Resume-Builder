---
phase: 01-foundation
plan: 02
type: execute
wave: 2
depends_on: ["01-foundation-01"]
files_modified:
  - lib/schema.ts
  - context/ResumeContext.tsx
  - hooks/usePersistence.ts
  - app/layout.tsx
autonomous: true
requirements:
  - PHASE1-STATE
  - PHASE1-PERSIST
must_haves:
  truths:
    - "Invalid resume data cannot be submitted or saved."
    - "Resume data persists after a browser refresh."
    - "Resume updates appear instantly in both Editor and Preview panes."
  artifacts:
    - path: "lib/schema.ts"
      provides: "Zod Resume Schema"
    - path: "context/ResumeContext.tsx"
      provides: "Global Resume state management"
  key_links:
    - from: "context/ResumeContext.tsx"
      to: "hooks/usePersistence.ts"
      via: "hook usage for side effects"
---

<objective>
Implement the global state management for the resume builder using React Context and Zod for schema validation, with automatic LocalStorage persistence.

Purpose: Provides a single source of truth for resume data that persists across page reloads.
Output: Typed resume state provider and persistence mechanism.
</objective>

<execution_context>
@C:/Users/kshir/.gemini/get-shit-done/workflows/execute-plan.md
@C:/Users/kshir/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-foundation/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Resume Zod Schema & Types</name>
  <files>lib/schema.ts, tests/schema.test.ts</files>
  <behavior>
    - Test that the schema validates correct resume objects (personal info, experience, education, skills).
    - Test that it rejects invalid data (e.g., missing required email, invalid dates).
    - Test that it handles empty arrays for sections correctly.
  </behavior>
  <action>
    - Define a comprehensive Zod schema in `lib/schema.ts` for the resume (Single-Column optimization).
    - Fields: `personalInfo` (name, email, phone, location, links), `summary` (optional), `experience` (array), `education` (array), `skills` (array), `projects` (array).
    - Export TypeScript types derived from the Zod schema.
    - Create a default/empty resume object for initial state.
  </action>
  <verify>
    <automated>npx vitest run tests/schema.test.ts</automated>
  </verify>
  <done>Zod schema and types defined, passing all validation tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: State Management & LocalStorage Persistence</name>
  <files>context/ResumeContext.tsx, hooks/usePersistence.ts, app/layout.tsx, tests/persistence.test.ts</files>
  <behavior>
    - Test that updating state via Context Provider results in the new state being stored in LocalStorage.
    - Test that the provider hydrates from LocalStorage on mount if data exists.
    - Test that invalid LocalStorage data (schema mismatch) falls back to the default state.
  </behavior>
  <action>
    - Create `context/ResumeContext.tsx` with a provider that manages the `resume` state.
    - Implement a `usePersistence` hook to sync the state to `localStorage` on changes.
    - Inside `ResumeProvider`, explicitly call `usePersistence(state)` to ensure all state changes are auto-saved.
    - Ensure hydration is handled correctly for Next.js (avoiding SSR mismatches).
    - Wrap the `app/layout.tsx` children in the `ResumeProvider`.
    - Provide a simple action to reset or clear the resume data.
  </action>
  <verify>
    <automated>npx vitest run tests/persistence.test.ts</automated>
  </verify>
  <done>Global resume state management and persistence implemented and verified with tests.</done>
</task>

</tasks>

<verification>
Verify that changes made to the resume state (manually via temporary UI) persist after a page refresh.
</verification>

<success_criteria>
- Zod schema enforces resume structure.
- LocalStorage contains the current resume state.
- Resume data is available in both Editor and Preview panes via Context.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-02-SUMMARY.md`
</output>
