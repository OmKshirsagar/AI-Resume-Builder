---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/styles/globals.css
  - src/components/layout/MainLayout.tsx
  - src/app/layout.tsx
autonomous: true
requirements:
  - PHASE1-INIT
  - PHASE1-LAYOUT
must_haves:
  truths:
    - "Tailwind 4 theme includes A4 dimensions (210mm x 297mm)."
    - "Split-pane layout persists its size across page refreshes without SSR flicker."
  artifacts:
    - path: "src/styles/globals.css"
      provides: "Tailwind 4 theme with A4 constants"
    - path: "src/components/layout/MainLayout.tsx"
      provides: "Persistent split-pane layout"
---

<objective>
Refine the project foundation by implementing Tailwind 4 theme variables for A4 dimensions and ensuring the split-pane layout persists its state across sessions using SSR-safe cookie handling.

Purpose: Professional-grade UI shell with no layout shifts.
Output: Persistent, theme-aware layout.
</objective>

<execution_context>
@C:/Users/kshir/.gemini/get-shit-done/workflows/execute-plan.md
@C:/Users/kshir/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tailwind 4 Theme Refinement</name>
  <files>src/styles/globals.css</files>
  <action>
    - Update `@theme` block in `src/styles/globals.css` with A4 constants:
      - `--a4-width: 210mm;`
      - `--a4-height: 297mm;`
    - Add semantic color variables for the resume editor and preview backgrounds.
    - Ensure font variables are correctly mapped for both display and print.
  </action>
  <verify>
    <automated>grep -q "\-\-a4-width" src/styles/globals.css</automated>
  </verify>
  <done>Tailwind 4 theme updated with A4 constants and semantic variables.</done>
</task>

<task type="auto">
  <name>Task 2: Persistent Split-Pane Layout (SSR-Safe)</name>
  <files>src/components/layout/MainLayout.tsx, src/app/layout.tsx</files>
  <action>
    - Implement `onLayout` callback in `MainLayout.tsx` to save the pane sizes into a cookie (e.g., `react-resizable-panels:layout`).
    - Use `next/headers` to read the cookie in the root `layout.tsx` or a Server Component wrapper.
    - Pass the default layout from the cookie to `MainLayout.tsx` as a prop to `PanelGroup`'s `defaultLayout`.
    - This ensures the layout is set on the server and prevents "layout jump" on hydration.
  </action>
  <verify>
    <automated>grep -q "cookies()" src/app/layout.tsx || grep -q "cookies()" src/components/layout/MainLayout.tsx</automated>
  </verify>
  <done>Layout persistence implemented via cookies, eliminating SSR flicker.</done>
</task>

</tasks>

<verification>
Run `npm run dev`, resize the panes, refresh the page. The panes should stay exactly where they were with NO sudden jump on load.
</verification>

<success_criteria>
- A4 dimensions are defined in CSS variables.
- Resizable panes persist their positions across refreshes.
- No layout shift (CLS) on page load due to pane resizing.
</success_criteria>

<output>
After completion, update `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
