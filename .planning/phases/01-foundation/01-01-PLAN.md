---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - app/layout.tsx
  - app/page.tsx
  - components/MainLayout.tsx
  - components/editor/EditorSkeleton.tsx
  - components/preview/PreviewSkeleton.tsx
  - vitest.config.ts
  - tests/setup.ts
autonomous: true
requirements:
  - PHASE1-INIT
  - PHASE1-LAYOUT
  - PHASE1-TEST-SETUP
must_haves:
  truths:
    - "Project initializes with Next.js, Tailwind, and Shadcn UI."
    - "The main layout uses a resizable split-pane with Editor on LHS and Preview on RHS."
    - "A testing environment (Vitest) is configured and working."
  artifacts:
    - path: "components/MainLayout.tsx"
      provides: "Resizable split-pane structure"
    - path: "vitest.config.ts"
      provides: "Testing configuration"
  key_links:
    - from: "app/page.tsx"
      to: "components/MainLayout.tsx"
      via: "import and render"
---

<objective>
Initialize the Next.js project foundation with Shadcn UI and Vitest, and implement the main resizable split-pane layout for the resume builder.

Purpose: Establishes the core UI shell and development environment.
Output: Working Next.js app with resizable panes and test scaffold.
</objective>

<execution_context>
@C:/Users/kshir/.gemini/get-shit-done/workflows/execute-plan.md
@C:/Users/kshir/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Project Initialization & Test Scaffold</name>
  <files>package.json, vitest.config.ts, tests/setup.ts</files>
  <action>
    - Initialize Next.js project if not already present.
    - Install dependencies: `lucide-react`, `clsx`, `tailwind-merge`.
    - Install Shadcn UI via `npx shadcn-ui@latest init` (choose default options, Slate theme).
    - Install `react-resizable-panels`.
    - Install dev dependencies for testing: `vitest`, `@vitejs/plugin-react`, `@testing-library/react`, `@testing-library/jest-dom`, `jsdom`.
    - Create `vitest.config.ts` and `tests/setup.ts` to configure the testing environment.
    - Create a simple smoke test `tests/smoke.test.ts` to verify Vitest works.
  </action>
  <verify>
    <automated>npx vitest run tests/smoke.test.ts</automated>
  </verify>
  <done>Next.js initialized, Shadcn configured, and Vitest running successfully.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Resizable Split-Pane Layout</name>
  <files>components/MainLayout.tsx, app/page.tsx, app/layout.tsx, tests/MainLayout.test.tsx</files>
  <behavior>
    - Test that `MainLayout` renders exactly two resizable panels (LHS and RHS).
    - Test that the resize handle is present between the panels.
    - Test that the layout defaults to a horizontal split.
  </behavior>
  <action>
    - Create `components/MainLayout.tsx` using `react-resizable-panels`.
    - Implement a horizontal split (PanelGroup direction="horizontal").
    - LHS panel should be the Editor (resizable, default 50%).
    - RHS panel should be the Preview (resizable, default 50%).
    - Add a `PanelResizeHandle` with styling (e.g., a subtle vertical line with hover effect).
    - Update `app/page.tsx` to use `MainLayout`.
    - Ensure `app/layout.tsx` has correct metadata and basic Tailwind body styles (e.g., h-screen, overflow-hidden).
    - Create `tests/MainLayout.test.tsx` to verify the split-pane renders correctly using `@testing-library/react`.
  </action>
  <verify>
    <automated>npx vitest run tests/MainLayout.test.tsx</automated>
  </verify>
  <done>Split-pane layout implemented with resizable handles, visible on the home page and verified by tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Editor & Preview Skeletons</name>
  <files>components/editor/EditorSkeleton.tsx, components/preview/PreviewSkeleton.tsx, components/MainLayout.tsx, tests/Skeletons.test.tsx</files>
  <behavior>
    - Test that `EditorSkeleton` renders within the LHS panel and is scrollable.
    - Test that `PreviewSkeleton` renders within the RHS panel.
  </behavior>
  <action>
    - Create `components/editor/EditorSkeleton.tsx`: A scrollable container with placeholder text/styling for the resume input fields.
    - Create `components/preview/PreviewSkeleton.tsx`: A container with a light background to represent the resume preview area.
    - Integrate these skeletons into `MainLayout.tsx` to verify the layout works visually.
    - Ensure both LHS and RHS are independently scrollable within their panes.
    - Create `tests/Skeletons.test.tsx` to verify that both skeletons render within the `MainLayout`.
  </action>
  <verify>
    <automated>npx vitest run tests/Skeletons.test.tsx</automated>
  </verify>
  <done>Editor and Preview skeletons created and rendered within the resizable layout, verified by tests.</done>
</task>

</tasks>

<verification>
Check if the local server starts without errors (`npm run dev`) and verifies the resizable panes function in the browser.
</verification>

<success_criteria>
- Project starts and renders the split-pane layout.
- Vitest smoke test passes.
- LHS and RHS panes are resizable via mouse interaction.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
