---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - src/app/layout.tsx
  - src/app/page.tsx
  - src/components/layout/MainLayout.tsx
  - src/components/editor/EditorSkeleton.tsx
  - src/components/preview/PreviewSkeleton.tsx
autonomous: true
requirements:
  - PHASE1-INIT
  - PHASE1-LAYOUT
must_haves:
  truths:
    - "Project uses Next.js 15 with Tailwind CSS 4."
    - "The main layout uses a resizable split-pane with Editor on LHS and Preview on RHS."
    - "Biome is used for linting and formatting."
  artifacts:
    - path: "src/components/layout/MainLayout.tsx"
      provides: "Resizable split-pane structure"
  key_links:
    - from: "src/app/page.tsx"
      to: "src/components/layout/MainLayout.tsx"
      via: "import and render"
---

<objective>
Update the project foundation and implement the main resizable split-pane layout for the resume builder using Tailwind 4 and react-resizable-panels.

Purpose: Establishes the core UI shell.
Output: Working Next.js app with resizable panes.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Project Cleanup & Dependency Installation</name>
  <files>package.json, src/app/page.tsx, src/app/layout.tsx</files>
  <action>
    - Install `react-resizable-panels` and `clsx`.
    - Clean up `src/app/page.tsx` and `src/app/layout.tsx` to remove the default T3 scaffolding.
    - Ensure Tailwind 4 is correctly configured (check `src/styles/globals.css`).
  </action>
  <done>Dependencies installed and T3 boiler plate removed.</done>
</task>

<task type="auto">
  <name>Task 2: Resizable Split-Pane Layout</name>
  <files>src/components/layout/MainLayout.tsx, src/app/page.tsx</files>
  <action>
    - Create `src/components/layout/MainLayout.tsx` using `react-resizable-panels`.
    - Implement a horizontal split (PanelGroup direction="horizontal").
    - LHS panel should be the Editor (resizable, default 50%).
    - RHS panel should be the Preview (resizable, default 50%).
    - Add a `PanelResizeHandle` with styling consistent with Tailwind 4.
    - Update `src/app/page.tsx` to use `MainLayout`.
  </action>
  <done>Split-pane layout implemented and visible on the home page.</done>
</task>

<task type="auto">
  <name>Task 3: Editor & Preview Skeletons</name>
  <files>src/components/editor/EditorSkeleton.tsx, src/components/preview/PreviewSkeleton.tsx, src/components/layout/MainLayout.tsx</files>
  <action>
    - Create `src/components/editor/EditorSkeleton.tsx`: A scrollable container with placeholder content for the editor.
    - Create `src/components/preview/PreviewSkeleton.tsx`: A container for the resume preview area.
    - Integrate these skeletons into `MainLayout.tsx`.
    - Ensure both LHS and RHS are independently scrollable within their panes.
  </action>
  <done>Editor and Preview skeletons created and rendered within the resizable layout.</done>
</task>

</tasks>

<verification>
Run `npm run dev` and verify the resizable panes function in the browser.
</verification>

<success_criteria>
- Project starts and renders the split-pane layout.
- LHS and RHS panes are resizable via mouse interaction.
- Editor and Preview skeletons are visible.
</success_criteria>

<output>
After completion, update `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
