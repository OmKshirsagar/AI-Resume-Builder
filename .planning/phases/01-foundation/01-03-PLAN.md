---
phase: 01-foundation
plan: 03
type: execute
wave: 1
depends_on: ["01-02"]
files_modified:
  - src/components/preview/A4Page.tsx
  - src/hooks/useScaleToFit.ts
  - src/hooks/usePageOverflow.ts
autonomous: true
requirements:
  - PHASE1-SCALING
  - PHASE1-OVERFLOW
must_haves:
  truths:
    - "Preview container matches A4 proportions."
    - "The preview scales automatically to fit its panel width."
    - "The preview detects content overflow beyond one page."
  artifacts:
    - path: "src/components/preview/A4Page.tsx"
      provides: "Scaled A4 container"
---

<objective>
Create the "One-Page" preview container with scaling logic and overflow detection using Tailwind 4.

Purpose: Ensures the resume always fits on one page and is visible regardless of screen size.
Output: Scaled A4 preview component with overflow warnings.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: A4 Container Component</name>
  <files>src/components/preview/A4Page.tsx</files>
  <action>
    - Create `src/components/preview/A4Page.tsx` with fixed physical proportions (210mm x 297mm).
    - Use Tailwind 4 for styling the page container.
    - Implement a wrapping container that handles the `transform: scale()` property.
  </action>
  <done>A4 container component created.</done>
</task>

<task type="auto">
  <name>Task 2: Scaling Logic (useScaleToFit)</name>
  <files>src/hooks/useScaleToFit.ts, src/components/preview/A4Page.tsx</files>
  <action>
    - Create `useScaleToFit` hook to calculate the scaling factor based on the parent container's width.
    - Apply the scaling factor to the `A4Page` component.
    - Ensure the preview is centered within the RHS panel.
  </action>
  <done>Scaling logic implemented and applied to the preview.</done>
</task>

<task type="auto">
  <name>Task 3: Overflow Detection (usePageOverflow)</name>
  <files>src/hooks/usePageOverflow.ts, src/components/preview/A4Page.tsx</files>
  <action>
    - Create `usePageOverflow` hook using `ResizeObserver` or scroll height comparison.
    - Implement a visual indicator (e.g., a red border or warning badge) when content exceeds the A4 page limit.
  </action>
  <done>Overflow detection implemented with visual feedback.</done>
</task>

</tasks>

<verification>
Test the scaling by resizing the browser window and the split-pane handle. Verify that a warning appears when adding excessive content to the resume.
</verification>

<success_criteria>
- A4 preview remains visible and centered at all panel widths.
- Content overflow triggers a visual warning.
- The preview maintains A4 aspect ratio (1:1.414).
</success_criteria>

<output>
After completion, update `.planning/phases/01-foundation/01-03-SUMMARY.md`
</output>
