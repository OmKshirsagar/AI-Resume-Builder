---
phase: 01-foundation
plan: 03
type: execute
wave: 1
depends_on: ["01-02"]
files_modified:
  - src/components/preview/A4Page.tsx
  - src/lib/scaling.ts
  - src/components/preview/PreviewPane.tsx
autonomous: true
requirements:
  - PHASE1-SCALING
  - PHASE1-OVERFLOW
must_haves:
  truths:
    - "The resume preview uses CSS transforms to scale an A4 container to fit its parent width."
    - "Components inside the preview use Container Queries for responsive styling."
    - "Overflow beyond the A4 page height is visually detected and flagged."
  artifacts:
    - path: "src/lib/scaling.ts"
      provides: "A4 scaling calculation logic"
    - path: "src/components/preview/A4Page.tsx"
      provides: "Scaled A4 container using container-type"
---

<objective>
Implement the pixel-perfect A4 resume preview engine using CSS transforms for scaling and Container Queries for internal responsiveness, ensuring the document always fits the preview pane.

Purpose: WYSIWYG preview that matches PDF output.
Output: High-fidelity, auto-scaling A4 preview component.
</objective>

<execution_context>
@C:/Users/kshir/.gemini/get-shit-done/workflows/execute-plan.md
@C:/Users/kshir/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement A4 Scaling Logic</name>
  <files>src/lib/scaling.ts, src/components/preview/A4Page.tsx</files>
  <action>
    - Create `src/lib/scaling.ts` with the `getA4Scale` utility function from research (96 DPI conversion).
    - Implement the `A4Page` component in `src/components/preview/A4Page.tsx`.
    - `A4Page` should have fixed physical dimensions using the CSS variables defined in `globals.css` (`--a4-width`, `--a4-height`).
    - Apply `container-type: inline-size` to the A4 page to enable container queries for its children.
    - Use `transform: scale()` with `transform-origin: top center` to fit the page into the preview pane.
  </action>
  <verify>
    <automated>grep -q "container-type: inline-size" src/components/preview/A4Page.tsx || grep -q "container-type: inline-size" src/styles/globals.css</automated>
  </verify>
  <done>A4 scaling engine implemented with Container Query support.</done>
</task>

<task type="auto">
  <name>Task 2: Dynamic Scaling & Overflow Detection</name>
  <files>src/components/preview/PreviewPane.tsx</files>
  <action>
    - Implement a wrapper component `PreviewPane.tsx` that uses a `ResizeObserver` (or similar hook) to measure the available width.
    - Pass the available width to the scaling logic to dynamically update the `transform: scale()` value on the `A4Page`.
    - Implement overflow detection by comparing the actual content height with the A4 page height (297mm).
    - Show a visual warning (e.g., a "Page Overflow" badge) when the content exceeds the 1-page limit.
  </action>
  <verify>
    <automated>grep -q "ResizeObserver" src/components/preview/PreviewPane.tsx || grep -q "useResizeObserver" src/components/preview/PreviewPane.tsx</automated>
  </verify>
  <done>Preview pane dynamically scales the resume and warns on page overflow.</done>
</task>

</tasks>

<verification>
Open the app, resize the split-pane. The resume should scale smoothly to fit. Add dummy content until it overflows and verify the warning appears.
</verification>

<success_criteria>
- Resume scales fluidly without blurring.
- A4 aspect ratio is maintained regardless of zoom level.
- Children of the A4 page can use `@container` queries for their styling.
- Overflow is detected and reported to the user.
</success_criteria>

<output>
After completion, update `.planning/phases/01-foundation/01-03-SUMMARY.md`
</output>
