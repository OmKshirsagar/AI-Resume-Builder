---
phase: 01-foundation
plan: 03
type: execute
wave: 3
depends_on: ["01-foundation-02"]
files_modified:
  - components/preview/A4Preview.tsx
  - hooks/useScaling.ts
  - hooks/useOverflow.ts
  - components/MainLayout.tsx
autonomous: true
requirements:
  - PHASE1-SCALING
  - PHASE1-OVERFLOW
must_haves:
  truths:
    - "The resume preview always visually maintains an A4 proportion, regardless of screen size."
    - "Preview shrinks or grows dynamically when resized to keep the entire A4 page visible."
    - "A visible warning appears instantly when resume content exceeds the single page boundary."
  artifacts:
    - path: "components/preview/A4Preview.tsx"
      provides: "A4 scaling container"
    - path: "hooks/useOverflow.ts"
      provides: "One-page boundary detection"
  key_links:
    - from: "components/preview/A4Preview.tsx"
      to: "hooks/useScaling.ts"
      via: "dynamic scale calculation"
---

<objective>
Implement the core "One-Page" preview engine, ensuring the resume always maintains an A4 aspect ratio, scales to fit the viewport, and detects content overflow.

Purpose: Enforces the single-page constraint visually and programmatically.
Output: Scalable A4 preview component with overflow detection.
</objective>

<execution_context>
@C:/Users/kshir/.gemini/get-shit-done/workflows/execute-plan.md
@C:/Users/kshir/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-foundation/01-02-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: A4 Scaling Container & useScaling Hook</name>
  <files>components/preview/A4Preview.tsx, hooks/useScaling.ts, components/MainLayout.tsx, tests/useScaling.test.ts</files>
  <behavior>
    - Test that `useScaling` returns a scale factor < 1 when container is smaller than A4 width.
    - Test that `useScaling` caps scale at 1 when container is larger than A4 width.
    - Test that `A4Preview` applies the scale factor via inline styles.
  </behavior>
  <action>
    - Define A4 dimensions: 210mm width, 297mm height (convert to pixels at a fixed DPI like 96 or 112).
    - Implement `hooks/useScaling.ts` which takes the parent container's width/height and returns a `scale` factor that fits the A4 page within it.
    - Create `tests/useScaling.test.ts` to assert the mathematical correctness of the scale factor calculation.
    - Create `components/preview/A4Preview.tsx`:
      - Inner container for content (fixed A4 size).
      - Outer container that applies `transform: scale(scaleFactor)` and `transform-origin: top center`.
      - Centering logic to keep the page aligned correctly.
    - Replace `PreviewSkeleton` in `MainLayout.tsx` with `A4Preview`.
  </action>
  <verify>
    <automated>npx vitest run tests/useScaling.test.ts</automated>
  </verify>
  <done>A4 preview component implemented with automatic scaling based on the RHS pane width.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Content Overflow Detection Hook</name>
  <files>hooks/useOverflow.ts, components/preview/A4Preview.tsx, tests/useOverflow.test.ts</files>
  <behavior>
    - Test that `useOverflow` returns false when `scrollHeight` <= `clientHeight` (or fixed max height).
    - Test that `useOverflow` returns true when content exceeds the max height limit.
  </behavior>
  <action>
    - Create `hooks/useOverflow.ts` that uses a `ResizeObserver` or checks `scrollHeight` vs `clientHeight`.
    - It should detect if the content within the A4 container exceeds the fixed A4 height.
    - Create `tests/useOverflow.test.ts` to mock ResizeObserver and ensure the hook correctly flips to true when height is exceeded.
    - Integrate the hook into `A4Preview.tsx`.
    - Add a visual warning (e.g., a red dotted line at the bottom or a subtle toast) when overflow is detected.
    - Ensure the overflow state is exposed via a prop or Context for future AI-driven content trimming.
  </action>
  <verify>
    <automated>npx vitest run tests/useOverflow.test.ts</automated>
  </verify>
  <done>Overflow detection hook implemented, providing visual feedback when content exceeds the single-page limit.</done>
</task>

</tasks>

<verification>
Manually test by resizing the RHS pane and verifying the A4 page scales correctly. Add mock content to the resume state to trigger the overflow warning.
</verification>

<success_criteria>
- A4 aspect ratio is preserved at all screen sizes.
- Scaling factor updates dynamically on window/pane resize.
- Overflow warning triggers when content is too long for one page.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-03-SUMMARY.md`
</output>
