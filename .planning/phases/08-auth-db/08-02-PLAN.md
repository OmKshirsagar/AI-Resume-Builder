---
phase: 08-auth-db
plan: 02
type: execute
wave: 2
depends_on: ["08-01"]
files_modified: [src/db/schema.ts, drizzle.config.ts, package.json]
autonomous: true
requirements: [DB-02, DB-03]

must_haves:
  truths:
    - "Relational 'Bullet Bank' schema correctly defined"
    - "Drizzle Kit can generate migrations"
    - "Migration successfully pushes to Turso"
  artifacts:
    - path: "src/db/schema.ts"
      provides: "Tables for users, resumes, experiences, bullets"
    - path: "drizzle.config.ts"
      provides: "Drizzle Kit configuration"
  key_links:
    - from: "src/db/schema.ts"
      to: "Turso"
      via: "drizzle-kit push:sqlite"
---

<objective>
Define the relational database schema for the 'Bullet Bank' and run the initial migration to Turso.

Purpose: Provide a persistent, relational data structure for multi-tenant resumes.
Output: Relational schema and initial database tables in Turso.
</objective>

<execution_context>
@C:/Users/kshir/.gemini/get-shit-done/workflows/execute-plan.md
@C:/Users/kshir/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-auth-db/08-01-SUMMARY.md
@.planning/phases/08-auth-db/08-RESEARCH.md
@src/db/index.ts
@package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define Bullet Bank Schema</name>
  <files>src/db/schema.ts</files>
  <action>
    Define the following tables in `src/db/schema.ts` using Drizzle's `sqlite-core`:
    - `users`: id (clerk id), email, imageUrl, createdAt
    - `resumes`: id, userId, title, personalInfo (JSON), design (JSON), isMaster (boolean)
    - `experiences`: id, resumeId, company, position, order (float/real)
    - `bullets`: id, experienceId, content, order (float/real), impactScore
    Follow the relational patterns and reordering strategies from 08-RESEARCH.md.
  </action>
  <verify>
    <automated>test -f src/db/schema.ts && grep "sqliteTable" src/db/schema.ts</automated>
  </verify>
  <done>Relational schema file created with all core tables.</done>
</task>

<task type="auto">
  <name>Task 2: Configure Drizzle Kit & Migration Scripts</name>
  <files>drizzle.config.ts, package.json</files>
  <action>
    1. Create `drizzle.config.ts` in the project root.
    2. Point it to `src/db/schema.ts` and use `libsql` driver.
    3. Use environment variables (via `process.env`) for `TURSO_DATABASE_URL` and `TURSO_AUTH_TOKEN`.
    4. Update `package.json` scripts:
       - `db:generate`: `drizzle-kit generate`
       - `db:migrate`: `drizzle-kit migrate`
       - `db:push`: `drizzle-kit push`
       - `db:studio`: `drizzle-kit studio`
  </action>
  <verify>
    <automated>grep "db:generate" package.json</automated>
  </verify>
  <done>Drizzle Kit configured and scripts added to package.json.</done>
</task>

<task type="auto">
  <name>Task 3: Run First Migration</name>
  <files>src/db/schema.ts</files>
  <action>
    Generate and push the initial migration to Turso.
    Run `npm run db:push` to sync the local schema with the remote Turso instance.
  </action>
  <verify>
    <automated>echo "Verification: Check Turso for tables users, resumes, experiences, bullets"</automated>
  </verify>
  <done>Database schema successfully applied to Turso.</done>
</task>

</tasks>

<verification>
Verify that `drizzle-kit studio` can successfully connect and display the tables.
</verification>

<success_criteria>
- Schema correctly represents the hierarchical relationship of Resume -> Experience -> Bullets.
- Migration runs without errors and creates the expected tables.
</success_criteria>

<output>
After completion, create `.planning/phases/08-auth-db/08-02-SUMMARY.md`
</output>
