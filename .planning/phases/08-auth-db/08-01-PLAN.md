---
phase: 08-auth-db
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [package.json, .env.example, src/env.js, middleware.ts, src/app/layout.tsx, src/db/index.ts]
autonomous: true
requirements: [AUTH-01, DB-01]
user_setup:
  - service: Clerk
    why: "User authentication"
    env_vars:
      - name: NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY
        source: "Clerk Dashboard -> API Keys"
      - name: CLERK_SECRET_KEY
        source: "Clerk Dashboard -> API Keys"
  - service: Turso
    why: "Cloud SQLite database"
    env_vars:
      - name: TURSO_DATABASE_URL
        source: "Turso CLI or Dashboard (libsql://...)"
      - name: TURSO_AUTH_TOKEN
        source: "Turso CLI or Dashboard"

must_haves:
  truths:
    - "Application is wrapped in ClerkProvider"
    - "Protected routes redirect to Clerk sign-in"
    - "Drizzle client successfully connects to Turso"
  artifacts:
    - path: "middleware.ts"
      provides: "Route protection"
    - path: "src/db/index.ts"
      provides: "Database client singleton"
  key_links:
    - from: "src/db/index.ts"
      to: "Turso"
      via: "@libsql/client"
---

<objective>
Establish the foundational infrastructure for Phase 8: Authentication with Clerk and Cloud Database connectivity with Turso and Drizzle ORM.

Purpose: Enable multi-tenant features and cloud persistence.
Output: Integrated Clerk auth, configured middleware, and a working Drizzle/Turso client.
</objective>

<execution_context>
@C:/Users/kshir/.gemini/get-shit-done/workflows/execute-plan.md
@C:/Users/kshir/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/08-auth-db/08-RESEARCH.md
@src/env.js
@package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install Dependencies & Configure Environment</name>
  <files>package.json, .env.example, src/env.js</files>
  <action>
    Install core auth and database libraries:
    `npm install @clerk/nextjs drizzle-orm @libsql/client svix`
    `npm install -D drizzle-kit`

    Update `.env.example` with:
    - NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY
    - CLERK_SECRET_KEY
    - TURSO_DATABASE_URL
    - TURSO_AUTH_TOKEN

    Update `src/env.js` to include these variables in the T3-Env schema, ensuring they are validated at runtime.
  </action>
  <verify>
    <automated>npm list @clerk/nextjs drizzle-orm @libsql/client</automated>
  </verify>
  <done>Dependencies installed and environment schema updated.</done>
</task>

<task type="auto">
  <name>Task 2: Configure Clerk Auth & Middleware</name>
  <files>middleware.ts, src/app/layout.tsx</files>
  <action>
    1. Create `middleware.ts` in the root (or `src/`) following the Next.js 15 async pattern from 08-RESEARCH.md.
    2. Protect all routes except "/", "/api/webhooks(.*)", and auth routes.
    3. Wrap the root `src/app/layout.tsx` with `<ClerkProvider>`.
  </action>
  <verify>
    <automated>test -f middleware.ts && grep "clerkMiddleware" middleware.ts</automated>
  </verify>
  <done>Middleware active and ClerkProvider integrated.</done>
</task>

<task type="auto">
  <name>Task 3: Initialize Drizzle & Turso Client</name>
  <files>src/db/index.ts</files>
  <action>
    Create a database client singleton in `src/db/index.ts`.
    Use `@libsql/client` to create a client with `TURSO_DATABASE_URL` and `TURSO_AUTH_TOKEN`.
    Export a `db` constant initialized with `drizzle(client)`.
    Ensure the client is reused across requests (singleton pattern).
  </action>
  <verify>
    <automated>test -f src/db/index.ts && grep "createClient" src/db/index.ts</automated>
  </verify>
  <done>Drizzle client available for use in the application.</done>
</task>

</tasks>

<verification>
Run `npm run build` to ensure no environment validation or dependency errors.
Check that middleware redirects to Clerk if accessing a protected route.
</verification>

<success_criteria>
- All required packages installed and versions match research recommendations.
- Clerk middleware correctly identifies public vs private routes.
- Drizzle client exports a working `db` instance.
</success_criteria>

<output>
After completion, create `.planning/phases/08-auth-db/08-01-SUMMARY.md`
</output>
