---
phase: 08-auth-db
plan: 03
type: execute
wave: 3
depends_on: ["08-02"]
files_modified: [src/app/api/webhooks/clerk/route.ts, src/components/auth/SyncObserver.tsx, src/store/useResumeStore.ts, src/app/page.tsx, src/app/actions/sync.ts]
autonomous: true
requirements: [SYNC-01, SYNC-02]
user_setup:
  - service: Clerk
    why: "Webhook sync"
    dashboard_config:
      - task: "Create Webhook for user.created"
        location: "Clerk Dashboard -> Webhooks"
        url: "https://your-domain.com/api/webhooks/clerk"

must_haves:
  truths:
    - "New users are automatically created in the database via webhooks"
    - "LocalStorage data is pushed to Turso upon first sign-in"
    - "Resume store reads from and writes to the database"
  artifacts:
    - path: "src/app/api/webhooks/clerk/route.ts"
      provides: "Webhook handler"
    - path: "src/components/auth/SyncObserver.tsx"
      provides: "Migration logic"
  key_links:
    - from: "src/components/auth/SyncObserver.tsx"
      to: "src/app/actions/sync.ts"
      via: "Server Action"
---

<objective>
Implement data synchronization logic to migrate existing localStorage data to the cloud and replace local-only persistence with remote database sync.

Purpose: Seamless transition from guest use to user account and multi-device persistence.
Output: Clerk-to-DB webhook sync, Client-side migration observer, and Cloud-backed state management.
</objective>

<execution_context>
@C:/Users/kshir/.gemini/get-shit-done/workflows/execute-plan.md
@C:/Users/kshir/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-auth-db/08-01-SUMMARY.md
@.planning/phases/08-auth-db/08-02-SUMMARY.md
@.planning/phases/08-auth-db/08-RESEARCH.md
@src/store/useResumeStore.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Clerk Webhook Handler</name>
  <files>src/app/api/webhooks/clerk/route.ts</files>
  <action>
    Implement a POST endpoint in `src/app/api/webhooks/clerk/route.ts` to handle Clerk's `user.created` event.
    Use `svix` to verify the webhook signature.
    Sync the user data (id, email) to the `users` table in Turso.
  </action>
  <verify>
    <automated>test -f src/app/api/webhooks/clerk/route.ts && grep "Webhook" src/app/api/webhooks/clerk/route.ts</automated>
  </verify>
  <done>User records automatically created in DB on sign-up.</done>
</task>

<task type="auto">
  <name>Task 2: Migration Action & Sync Observer</name>
  <files>src/app/actions/sync.ts, src/components/auth/SyncObserver.tsx, src/app/page.tsx</files>
  <action>
    1. Create a server action `syncResumesAction` in `src/app/actions/sync.ts` that takes JSON resume data and upserts it into the Turso database for the current authenticated user.
    2. Create a `SyncObserver` client component in `src/components/auth/SyncObserver.tsx` following the pattern in 08-RESEARCH.md.
    3. Mount `SyncObserver` in the main application page (or a global layout after auth).
    4. Ensure it triggers only once per user session and clears `localStorage` after successful sync.
  </action>
  <verify>
    <automated>grep "localStorage.getItem" src/components/auth/SyncObserver.tsx</automated>
  </verify>
  <done>Existing local data migrated to Turso upon sign-in.</done>
</task>

<task type="auto">
  <name>Task 3: Refactor Store for Cloud Persistence</name>
  <files>src/store/useResumeStore.ts</files>
  <action>
    Refactor `useResumeStore.ts` to incorporate cloud persistence:
    1. If a user is logged in, fetch their resume data from Turso on mount.
    2. Update store actions to call server actions (or API routes) for persistence instead of relying purely on zustand's `persist` middleware with `localStorage`.
    3. Implement a debounced "Auto-Save" indicator for cloud sync status.
  </action>
  <verify>
    <automated>grep -v "persist" src/store/useResumeStore.ts | grep "save"</automated>
  </verify>
  <done>Application state synchronized with Turso database.</done>
</task>

</tasks>

<verification>
Test the flow: Create a resume as a guest -> Sign in -> Verify data is migrated to the cloud -> Refresh on a different device (if possible) to see data persist.
</verification>

<success_criteria>
- Webhook correctly identifies and syncs user profiles.
- Migration observer successfully moves localStorage data to DB.
- Store actions correctly update the database.
</success_criteria>

<output>
After completion, create `.planning/phases/08-auth-db/08-03-SUMMARY.md`
</output>
