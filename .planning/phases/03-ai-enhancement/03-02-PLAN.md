---
phase: 03-ai-enhancement
plan: 02
type: execute
wave: 2
depends_on: [03-01]
files_modified: [src/app/actions/tailor.ts, src/components/ai/JobTailorModal.tsx]
autonomous: true
requirements: [AI-02, AI-03, AI-04]

must_haves:
  truths:
    - "User can paste a Job Description (JD) into a modal"
    - "AI identifies core competencies and gaps between resume and JD"
    - "AI generates tailored suggestions for the summary and key experience bullets"
    - "Suggestions can be reviewed and applied to the resume store"
  artifacts:
    - path: "src/app/actions/tailor.ts"
      provides: "Server action for multi-step job tailoring suggestions"
    - path: "src/components/ai/JobTailorModal.tsx"
      provides: "Interactive UI for JD entry and suggestion review"
  key_links:
    - from: "src/components/ai/JobTailorModal.tsx"
      to: "src/app/actions/tailor.ts"
      via: "Server Action call with streamObject"
---

<objective>
Implement the "Job Tailor" flow which takes a Job Description and the current resume to provide AI-generated alignment suggestions for Summary and Experience sections.

Purpose: Automate the process of tailoring a resume for specific job applications.
Output: Tailoring server action and Modal UI for suggestion review.
</objective>

<execution_context>
@C:/Users/kshir/.gemini/get-shit-done/workflows/execute-plan.md
@C:/Users/kshir/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-ai-enhancement/03-RESEARCH.md
@src/schemas/resume.ts
@src/app/actions/refine.ts
@src/lib/ai/prompts.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Job Tailor Server Action with Structured Output</name>
  <files>src/app/actions/tailor.ts</files>
  <action>
    1. Create `src/app/actions/tailor.ts`:
       - Use `streamObject` from Vercel AI SDK to return structured suggestions.
       - Define a Zod schema for suggestions: `{ summary: string, experienceChanges: Array<{ id: string, originalBullet: string, newBullet: string, reasoning: string }> }`.
       - The prompt in `src/lib/ai/prompts.ts` should:
         - Instruct Gemini 3 Flash to perform a multi-step analysis internally: Extract keywords -> Analyze Gaps -> Rewrite.
         - Focus on aligning the Summary and high-impact Experience bullets.
         - Ensure the output strictly follows the Zod schema for easy UI consumption.
       - Return a streamable object value using `createStreamableValue`.
  </action>
  <verify>
    <automated>Check if src/app/actions/tailor.ts uses streamObject and createStreamableValue.</automated>
  </verify>
  <done>Server action correctly streams structured tailoring suggestions based on a JD and current resume data.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Job Tailor Modal and Review UI</name>
  <files>src/components/ai/JobTailorModal.tsx</files>
  <action>
    1. Create `src/components/ai/JobTailorModal.tsx` (Client Component):
       - Use Shadcn `Dialog` for the modal.
       - Stage 1: Textarea for pasting JD.
       - Stage 2: Streaming review of suggestions. Display "Before" and "After" for each suggested change.
       - Stage 3: Selection UI. Allow user to toggle which changes they want to apply.
       - "Apply Selected" button: Updates the Zustand store using the IDs provided in the suggestion object.
    2. Add a "Tailor for Job" button in the `MainLayout` or `ResumeBuilder` toolbar to trigger this modal.
  </action>
  <verify>
    <automated>Check if src/components/ai/JobTailorModal.tsx handles streaming object updates and updates the Zustand store.</automated>
  </verify>
  <done>Job Tailor Modal allows users to input a JD, review AI-suggested changes, and apply them to their resume.</done>
</task>

</tasks>

<verification>
Manual verification: Open the "Tailor for Job" modal, paste a relevant JD, wait for suggestions to stream in, review the suggested Summary and Bullet changes, and click "Apply Selected". Verify the Resume Store is updated.
</verification>

<success_criteria>
- AI suggestions are relevant to the provided JD.
- Structured streaming ensures the UI can render individual changes as they arrive.
- User retains full control over which suggestions are applied.
- Integration with existing Resume Schema (via IDs).
</success_criteria>

<output>
After completion, create `.planning/phases/03-ai-enhancement/03-02-SUMMARY.md`
</output>
