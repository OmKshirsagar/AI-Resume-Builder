---
phase: 03-ai-enhancement
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/lib/ai/prompts.ts, src/app/actions/refine.ts, src/components/ai/RefineButton.tsx]
autonomous: true
requirements: [AI-01, AI-03, AI-04]

must_haves:
  truths:
    - "User can trigger refinement on an experience bullet or summary"
    - "AI enhancement follows the X-Y-Z formula (Accomplished [X] as measured by [Y], by doing [Z])"
    - "The enhanced text streams into the UI in real-time"
  artifacts:
    - path: "src/lib/ai/prompts.ts"
      provides: "System prompts for X-Y-Z refinement"
    - path: "src/app/actions/refine.ts"
      provides: "Server action using streamText and createStreamableValue"
    - path: "src/components/ai/RefineButton.tsx"
      provides: "Interactive UI for triggering and displaying streamed refinement"
  key_links:
    - from: "src/components/ai/RefineButton.tsx"
      to: "src/app/actions/refine.ts"
      via: "Server Action call with createStreamableValue"
---

<objective>
Implement the "Refine Section" feature allowing users to enhance individual resume bullets or summaries using Gemini 3 Flash and the X-Y-Z formula with real-time streaming.

Purpose: High-impact resume content generation with immediate feedback.
Output: Refinement server action and reusable RefineButton component.
</objective>

<execution_context>
@C:/Users/kshir/.gemini/get-shit-done/workflows/execute-plan.md
@C:/Users/kshir/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-ai-enhancement/03-RESEARCH.md
@src/schemas/resume.ts
@src/app/actions/extract.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define AI Prompts and Refine Server Action</name>
  <files>src/lib/ai/prompts.ts, src/app/actions/refine.ts</files>
  <action>
    1. Create `src/lib/ai/prompts.ts` with a constant `REFINE_SYSTEM_PROMPT` that:
       - Instructs Gemini 3 Flash to use the X-Y-Z formula: "Accomplished [X] as measured by [Y], by doing [Z]".
       - Uses few-shot examples of weak vs strong bullets.
       - Handles missing metrics by using `[METRIC]` placeholders.
       - Enforces factual integrity (no hallucinating company names or dates).
    2. Create `src/app/actions/refine.ts` as a Server Action:
       - Use `createGoogleGenerativeAI` with `gemini-3-flash`.
       - Implement `refineText(originalText: string)` using `streamText` from Vercel AI SDK.
       - Use `createStreamableValue` from `ai/rsc` to wrap the stream for React 19 compatibility.
       - Ensure the action returns the streamable value object.
  </action>
  <verify>
    <automated>Check if src/app/actions/refine.ts exports a function and imports streamText/createStreamableValue.</automated>
  </verify>
  <done>Server action correctly streams X-Y-Z enhanced text using Gemini 3 Flash.</done>
</task>

<task type="auto">
  <name>Task 2: Implement RefineButton UI with Streaming</name>
  <files>src/components/ai/RefineButton.tsx</files>
  <action>
    1. Create `src/components/ai/RefineButton.tsx` (Client Component):
       - Props: `text: string`, `onApply: (newText: string) => void`.
       - Use `readStreamableValue` from `ai/rsc` to consume the stream from `refineText`.
       - Show a "Refining..." state with the streaming text appearing in a preview/tooltip or temporary area.
       - Provide "Apply" and "Discard" buttons once streaming finishes.
       - Styling: Use Shadcn UI (Button, Tooltip, or Popover) and Lucide icons (Sparkles).
    2. Integration hint: This component will be placed next to bullet points in the experience editor and the summary textarea.
  </action>
  <verify>
    <automated>Check if src/components/ai/RefineButton.tsx uses readStreamableValue and calls refineText.</automated>
  </verify>
  <done>RefineButton component allows users to preview and apply AI-enhanced text.</done>
</task>

</tasks>

<verification>
Manual verification: Open the editor, click the sparkles icon next to a bullet, observe the X-Y-Z refinement streaming in, and click "Apply" to update the resume state.
</verification>

<success_criteria>
- AI-generated refinements follow X-Y-Z formula.
- UI shows streaming progress without blocking the main thread.
- Successful integration with Zustand store (via onApply callback).
</success_criteria>

<output>
After completion, create `.planning/phases/03-ai-enhancement/03-01-SUMMARY.md`
</output>
