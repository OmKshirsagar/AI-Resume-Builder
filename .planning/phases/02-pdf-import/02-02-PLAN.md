---
phase: 02-pdf-import
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified: [src/app/page.tsx, src/components/editor/ParsedDataReview.tsx, src/store/useResumeStore.ts]
autonomous: true
requirements: [STORE-INTEGRATION, DATA-REVIEW-UI]
must_haves:
  truths:
    - "Extracted JSON is displayed for user review"
    - "User can 'Accept' or 'Cancel' applying the extracted data"
    - "Accepted data updates the Zustand store (with generated IDs)"
    - "Store persistence (localStorage) works for parsed data"
  artifacts:
    - path: "src/components/editor/ParsedDataReview.tsx"
      provides: "UI for reviewing and confirming extracted resume data"
  key_links:
    - from: "src/components/editor/ParsedDataReview.tsx"
      to: "src/store/useResumeStore.ts"
      via: "setResume method"
---

<objective>
Integrate the AI extraction pipeline with the Zustand store and build the review UI. This ensures that users can verify the parsed data before it's applied to their active resume state, preventing accidental overwrites.

Purpose: Provide a safety net and seamless integration for parsed data.
Output: Review modal/section and complete store integration.
</objective>

<execution_context>
@C:/Users/kshir/.gemini/get-shit-done/workflows/execute-plan.md
@C:/Users/kshir/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-pdf-import/02-01-SUMMARY.md
@src/store/useResumeStore.ts
@src/schemas/resume.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Parsed Data Review Component</name>
  <files>src/components/editor/ParsedDataReview.tsx</files>
  <action>
    1. Create a `ParsedDataReview` component that takes `data: Partial<ResumeData>` and `onConfirm`, `onCancel` callbacks.
    2. Build a clean, readable UI to display the extracted sections:
       - Summary, Personal Info.
       - Experience list.
       - Education list.
       - Skills and Projects.
    3. Use a modal or a conditional overlay in the editor pane.
    4. Implement "Accept" logic:
       - Call `onConfirm` with the validated data.
    5. Implement "Cancel" logic:
       - Call `onCancel` to clear the extraction state.
    6. Ensure the component uses Biome for formatting.
  </action>
  <verify>
    <automated>Verify component file exists and handles props correctly.</automated>
  </verify>
  <done>Review UI is visually functional and handles user actions (Mock data can be used for testing).</done>
</task>

<task type="auto">
  <name>Task 2: Integrate Pipeline in Main Page and Store</name>
  <files>src/app/page.tsx, src/store/useResumeStore.ts</files>
  <action>
    1. Open `src/app/page.tsx`.
    2. Add state to manage `pendingExtractedData` (the JSON from PDFUpload).
    3. Update the layout:
       - Insert `PDFUpload` at the top of the editor LHS.
       - Show `ParsedDataReview` when `pendingExtractedData` is present.
    4. Implement the mapping logic for `onConfirm`:
       - Use `crypto.randomUUID()` to generate IDs for each experience, education, and skill item from the AI output (since AI won't generate valid internal IDs).
       - Validate the mapped data against `ResumeSchema`.
       - Call `setResume(validatedData)` from the Zustand store.
       - Clear `pendingExtractedData`.
    5. Verify that `localStorage` updates correctly after applying the data.
  </action>
  <verify>
    <automated>Check for useResumeStore hook usage in src/app/page.tsx.</automated>
  </verify>
  <done>PDF upload -> AI extraction -> Review UI -> Zustand Store flow is fully connected.</done>
</task>

</tasks>

<verification>
- Upload a sample PDF and verify the review UI appears.
- Confirm "Accept" updates the preview on the RHS.
- Refresh the page and ensure the data persists (localStorage).
</verification>

<success_criteria>
- User can successfully import an old resume into the builder.
- Extracted data is editable and persists after approval.
- Layout remains responsive during and after the import process.
</success_criteria>

<output>
After completion, create `.planning/phases/02-pdf-import/02-02-SUMMARY.md`
</output>
