---
phase: 02-pdf-import
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [package.json, src/app/actions/extract.ts, src/components/editor/PDFUpload.tsx]
autonomous: true
requirements: [PDF-IMPORT-UI, AI-EXTRACTION-SERVICE]
user_setup:
  - service: google-gemini
    why: "AI-powered data extraction from PDF"
    env_vars:
      - name: GOOGLE_GENERATIVE_AI_API_KEY
        source: "Google AI Studio -> Get API key"

must_haves:
  truths:
    - "User can select a PDF file for upload"
    - "Server Action receives PDF buffer and calls Gemini 1.5 Flash"
    - "AI returns structured JSON matching ResumeSchema (excluding IDs)"
  artifacts:
    - path: "src/app/actions/extract.ts"
      provides: "Server Action for AI extraction"
    - path: "src/components/editor/PDFUpload.tsx"
      provides: "UI for PDF file selection and upload trigger"
  key_links:
    - from: "src/components/editor/PDFUpload.tsx"
      to: "src/app/actions/extract.ts"
      via: "React Server Action call"
---

<objective>
Implement the core PDF upload and AI extraction engine. This involves creating a Server Action that leverages the Vercel AI SDK and Gemini 1.5 Flash to parse PDF resumes into structured JSON data.

Purpose: Automate the resume building process by importing existing data.
Output: Working extraction pipeline from PDF file to validated JSON object.
</objective>

<execution_context>
@C:/Users/kshir/.gemini/get-shit-done/workflows/execute-plan.md
@C:/Users/kshir/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-pdf-import/02-RESEARCH.md
@src/schemas/resume.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Setup AI SDK and Extraction Server Action</name>
  <files>package.json, src/app/actions/extract.ts</files>
  <action>
    1. Install dependencies: `npm install ai @ai-sdk/google`.
    2. Create `src/app/actions/extract.ts` as a Server Action.
    3. Implement `extractResumeFromPDF` function:
       - Accept `FormData` containing the PDF file.
       - Convert PDF file to `Buffer`.
       - Use `generateObject` from `ai` package with `google('gemini-1.5-flash')`.
       - Use a modified version of `ResumeSchema` (or just the fields themselves) for extraction, ensuring IDs are NOT required from the AI.
       - Return the structured JSON data.
    4. Handle errors (e.g., file too large, AI failure) and return a Result type `{ success: true, data: ... } | { success: false, error: string }`.
  </action>
  <verify>
    <automated>Check if src/app/actions/extract.ts exports a function and imports from 'ai' and '@ai-sdk/google'.</automated>
  </verify>
  <done>Server Action correctly calls Gemini and returns structured data (mock/test with a small PDF if API key available, or verify logic).</done>
</task>

<task type="auto">
  <name>Task 2: Build PDF Upload Component</name>
  <files>src/components/editor/PDFUpload.tsx</files>
  <action>
    1. Create a Client Component `PDFUpload` that uses an HTML file input.
    2. Style it using Tailwind 4 (matches project theme).
    3. Implement file selection logic:
       - Accept only `.pdf` files.
       - Limit size (e.g., 4MB per research).
    4. On upload, trigger the `extractResumeFromPDF` Server Action.
    5. Handle loading states (use a spinner or progress bar).
    6. Emit the extracted data via a prop `onExtracted(data: ResumeData)`.
    7. Ensure Biome is used for formatting.
  </action>
  <verify>
    <automated>Verify file exists and uses 'use client' directive.</automated>
  </verify>
  <done>Component allows file selection and triggers the server action on command.</done>
</task>

</tasks>

<verification>
- Run `npm run lint` (Biome) to ensure code quality.
- Verify Server Action can be called from a test script or the UI component.
</verification>

<success_criteria>
- PDF file can be uploaded via the UI.
- Server Action successfully processes a PDF buffer through Gemini (requires API key).
- Structured data is returned to the client component.
</success_criteria>

<output>
After completion, create `.planning/phases/02-pdf-import/02-01-SUMMARY.md`
</output>
