---
phase: 09-local-parser
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [package.json, next.config.js, src/lib/pdf-parser.ts]
autonomous: true
requirements: [PDF-EXTRACTION]
must_haves:
  truths:
    - "pdf2json is installed and configured for Next.js server components"
    - "PDF text is extracted with spatial awareness (preserving columns)"
  artifacts:
    - path: "src/lib/pdf-parser.ts"
      provides: "Layout-aware PDF text extraction"
---

<objective>
Install pdf2json and implement a coordinate-aware parsing utility to handle multi-column resume layouts.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Install dependencies and configure Next.js</name>
  <files>package.json, next.config.js</files>
  <action>
    - Install pdf2json: `npm install pdf2json`
    - Update `next.config.js` to add 'pdf2json' to `serverExternalPackages` to ensure it runs correctly in the Node.js runtime.
  </action>
  <verify>npm list pdf2json && next build (check for config errors)</verify>
  <done>Environment ready for local PDF parsing</done>
</task>

<task type="auto">
  <name>Task 2: Implement Layout-Aware Parser</name>
  <files>src/lib/pdf-parser.ts</files>
  <action>
    Create `parsePDF` utility using `pdf2json`:
    - Implement "Layout Reassembly" algorithm:
      1. Extract all text objects with {x, y, text}.
      2. Group objects into columns by X-coordinate (tolerance ≈ 2.0 units).
      3. Sort columns from Left to Right.
      4. Within each group, sort blocks by Y-coordinate (Top to Bottom).
      5. Join text with double-newlines between blocks.
    - Export `parsePDF(buffer: Buffer): Promise<string>`.
  </action>
  <verify>Create a test script or use a sample PDF to verify extracted text preserves column order.</verify>
  <done>Reliable text extraction for complex resume layouts is available.</done>
</task>

</tasks>

<success_criteria>
Successfully extracts text from a two-column PDF in a logical reading order (Sidebar content followed by Main content or vice versa).
</success_criteria>

<output>
After completion, create `.planning/phases/09-local-parser/09-01-SUMMARY.md`
</output>
