---
phase: 09-local-parser
plan: 02
type: execute
wave: 2
depends_on: [09-01]
files_modified: [src/app/actions/extract.ts, src/app/actions/resume.ts, src/components/editor/PDFUpload.tsx]
autonomous: true
requirements: [PDF-EXTRACTION]
---

<objective>
Refactor the extraction pipeline to use local parsing (reducing token costs) and implement file-hash-based deduplication.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Refactor Extraction Action</name>
  <files>src/app/actions/extract.ts</files>
  <action>
    - Update `extractResumeFromPDF` to accept an optional `fileHash`.
    - Use `parsePDF` from `src/lib/pdf-parser.ts` to convert the PDF buffer to structured text.
    - Update the Gemini prompt to receive the structured text instead of the raw PDF file.
    - This removes the need for Gemini's native PDF support, drastically reducing payload size and token usage.
  </action>
  <verify>Upload a PDF and confirm structured data is still correctly extracted into ResumeData schema.</verify>
  <done>Extraction uses local text parsing and costs fewer tokens.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Deduplication Logic</name>
  <files>src/app/actions/resume.ts, src/components/editor/PDFUpload.tsx</files>
  <action>
    - Create `checkExistingResume(hash: string)` in `src/app/actions/resume.ts` to query the `resumes` table for a matching `fileHash`.
    - Update `PDFUpload.tsx` to calculate SHA-256 hash using `crypto.subtle.digest` before upload.
    - If a match is found, retrieve the existing resume data instead of re-parsing.
    - Pass the hash to the extraction action if no match is found to store it for future use.
  </action>
  <verify>Upload the same PDF twice; the second time should return the cached result almost instantly.</verify>
  <done>System prevents redundant AI processing of identical files.</done>
</task>

</tasks>

<success_criteria>
- AI extraction works using text input instead of file input.
- Identical files are detected via hash, skipping the expensive extraction step.
</success_criteria>

<output>
After completion, create `.planning/phases/09-local-parser/09-02-SUMMARY.md`
</output>
