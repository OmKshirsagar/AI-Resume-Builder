---
phase: 06-pdf-export
plan: 02
type: execute
wave: 2
depends_on: ["06-01"]
files_modified: [
  "package.json",
  "src/components/export/PDFDocument.tsx",
  "src/components/export/DownloadButton.tsx",
  "src/components/ResumeBuilder.tsx"
]
autonomous: true
requirements: [PHASE6-PDF-EXPORT, PHASE6-PREVIEW-SYNC]

must_haves:
  truths:
    - "User can download a high-quality PDF version of their resume"
    - "Downloaded PDF visually matches the web preview (Layout, Templates, Smart List)"
    - "PDF output is ATS-friendly (selectable text, proper document structure)"
  artifacts:
    - path: "src/components/export/PDFDocument.tsx"
      provides: "PDF layout mirroring ResumeRenderer using react-pdf primitives"
    - path: "src/components/export/DownloadButton.tsx"
      provides: "Blob generation and file-saver integration"
  key_links:
    - from: "src/components/export/DownloadButton.tsx"
      to: "@react-pdf/renderer"
      via: "pdf().toBlob() function call"
      pattern: "pdf.*doc.*toBlob"
---

<objective>
Implement high-quality PDF export that mirrors the web preview using @react-pdf/renderer.

Purpose: Provide users with a downloadable, ATS-friendly document that preserves the AI-optimized content and visual styling chosen by the system.
Output: A functional PDF export pipeline with a download button in the application header.
</objective>

<execution_context>
@C:/Users/kshir/.gemini/get-shit-done/workflows/execute-plan.md
@C:/Users/kshir/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/components/preview/ResumeRenderer.tsx
@src/components/ResumeBuilder.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Setup PDF Dependencies and Primitives</name>
  <files>package.json, src/components/export/PDFDocument.tsx</files>
  <action>
    1. Install dependencies: `npm install @react-pdf/renderer react-pdf-tailwind file-saver` and `npm install -D @types/file-saver`.
    2. Create `src/components/export/PDFDocument.tsx`.
    3. Define a `PDFDocument` component that accepts `data: ResumeData`.
    4. Set up `react-pdf-tailwind` with the project's theme (colors, etc.).
    5. Register standard fonts (e.g., Inter/Roboto) or use base Helvetica if CDN fonts are unavailable.
    6. Implement the basic `Document` and `Page` structure (A4 size).
  </action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <done>PDF infrastructure and base document structure created.</done>
</task>

<task type="auto">
  <name>Task 2: Mirror ResumeRenderer in PDF Primitives</name>
  <files>src/components/export/PDFDocument.tsx</files>
  <action>
    Implement all sections from `ResumeRenderer.tsx` using `@react-pdf/renderer` components (`View`, `Text`):
    1. Header: Name and contact info with proper alignment.
    2. Summary: Italicized block.
    3. Layout Engine: Mirror "classic" vs "sidebar" layouts using Flexbox (since `react-pdf` doesn't support Grid).
    4. Sections: Experience, Education, Skills, Projects, and Custom Sections.
    5. Smart List: Port the horizontal bullet logic to a PDF-friendly implementation.
    6. Dynamic Styles: Ensure `fontSize` and `lineHeight` from `design` schema are respected.
    7. Use `react-pdf-tailwind` to apply styling consistently.
  </action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <done>PDF document mirroring web preview layouts completed.</done>
</task>

<task type="auto">
  <name>Task 3: Implement Download Flow and Header Button</name>
  <files>src/components/export/DownloadButton.tsx, src/components/ResumeBuilder.tsx</files>
  <action>
    1. Create `src/components/export/DownloadButton.tsx` that:
       - Accepts `data: ResumeData`.
       - Uses `pdf(doc).toBlob()` to generate the PDF blob on click.
       - Uses `file-saver`'s `saveAs` to download the file.
       - Shows a loading state (spinner/loading text) while generating.
    2. Update `ResumeBuilder.tsx`:
       - Import `DownloadButton`.
       - Place the button in the header next to the AI action buttons.
       - Pass `activeData` to the button.
    3. Run Biome check and fix formatting.
  </action>
  <verify>
    <automated>npm run check</automated>
  </verify>
  <done>Download functionality integrated into the UI.</done>
</task>

</tasks>

<verification>
- Verify that clicking "Download PDF" generates a file named `{FullName}_Resume.pdf`.
- Verify that the PDF contains selectable text (is not an image).
- Verify that the PDF layout changes when switching templates (e.g., Sidebar vs Classic).
- Verify that the PDF styling (font size, colors) matches the web preview.
</verification>

<success_criteria>
- PDF generation completes in &lt; 2 seconds for a standard resume.
- PDF visually mirrors the React-based preview.
- File downloading works reliably across browsers via file-saver.
- PDF is optimized for ATS readability.
</success_criteria>

<output>
After completion, create `.planning/phases/06-pdf-export/06-02-SUMMARY.md`
</output>
