---
phase: 06-pdf-export
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [
  "src/components/editor/ResumeEditor.tsx",
  "src/components/editor/forms/PersonalInfoForm.tsx",
  "src/components/editor/forms/ExperienceForm.tsx",
  "src/components/editor/forms/EducationForm.tsx",
  "src/components/editor/forms/SkillsForm.tsx",
  "src/components/editor/forms/ProjectsForm.tsx",
  "src/components/editor/forms/CustomSectionsForm.tsx",
  "src/components/ResumeBuilder.tsx"
]
autonomous: true
requirements: [PHASE6-EDITOR-UI, PHASE6-PREVIEW-SYNC]

must_haves:
  truths:
    - "User can edit all resume sections via a robust form interface"
    - "Preview updates automatically as user types (debounced sync to store)"
    - "User can add and remove items in Experience and Education sections"
    - "Form validation ensures required fields like Full Name are present"
  artifacts:
    - path: "src/components/editor/ResumeEditor.tsx"
      provides: "Main FormProvider wrapper and debounced sync logic"
    - path: "src/components/editor/forms/ExperienceForm.tsx"
      provides: "List management for work history using useFieldArray"
  key_links:
    - from: "src/components/editor/ResumeEditor.tsx"
      to: "src/store/useResumeStore.ts"
      via: "debounced setOriginal call"
      pattern: "setTimeout.*setOriginal.*400"
---

<objective>
Refactor the Editor UI to use React Hook Form for robust state management and deep syncing with the Zustand store.

Purpose: Replace fragmented inline inputs with a unified, validated form system that maintains data integrity and provides a smooth editing experience.
Output: A set of modular form components integrated into a main Editor wrapper that auto-saves to the global store.
</objective>

<execution_context>
@C:/Users/kshir/.gemini/get-shit-done/workflows/execute-plan.md
@C:/Users/kshir/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/schemas/resume.ts
@src/store/useResumeStore.ts
@src/components/ResumeBuilder.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Setup ResumeEditor wrapper with FormProvider and Debounced Sync</name>
  <files>src/components/editor/ResumeEditor.tsx</files>
  <action>
    Create a new `ResumeEditor.tsx` component that:
    1. Initializes `useForm<ResumeData>` with the `zodResolver(ResumeSchema)`.
    2. Wraps children in `<FormProvider>`.
    3. Accepts `data: ResumeData` and `onSync: (data: ResumeData) => void` as props.
    4. Implements a `useEffect` that watches form changes using `methods.watch()` and calls `onSync` with a 400ms debounce.
    5. Handles `methods.reset(data)` when `data` changes externally (e.g., after AI extraction) using a ref to prevent circular sync loops.
    6. Uses Biome for formatting.
  </action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <done>ResumeEditor exists with FormProvider and debounced watcher.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Core Form Sections (Personal, Experience, Education)</name>
  <files>
    src/components/editor/forms/PersonalInfoForm.tsx,
    src/components/editor/forms/ExperienceForm.tsx,
    src/components/editor/forms/EducationForm.tsx
  </files>
  <action>
    Create modular form components using `useFormContext` and `useFieldArray`:
    1. `PersonalInfoForm.tsx`: Inputs for fullName, email, phone, location, summary.
    2. `ExperienceForm.tsx`: Uses `useFieldArray` for `experience`. Includes fields for company, position, dates, location, and a nested list/textarea for description bullets. Add "Add Experience" and "Remove" buttons.
    3. `EducationForm.tsx`: Uses `useFieldArray` for `education`. Includes fields for school, degree, dates, and "Add Education" button.
    4. Ensure all inputs are correctly registered with their paths (e.g., `personalInfo.fullName`).
    5. Re-use `RefineButton` for summary and description fields.
  </action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <done>Core sections implemented as modular form components.</done>
</task>

<task type="auto">
  <name>Task 3: Implement Remaining Sections and Integrate in ResumeBuilder</name>
  <files>
    src/components/editor/forms/SkillsForm.tsx,
    src/components/editor/forms/ProjectsForm.tsx,
    src/components/editor/forms/CustomSectionsForm.tsx,
    src/components/ResumeBuilder.tsx
  </files>
  <action>
    1. Create `SkillsForm.tsx` and `ProjectsForm.tsx` using `useFieldArray`.
    2. Create `CustomSectionsForm.tsx` by refactoring existing `CustomSections.tsx` to use `useFormContext` (removing local `useForm`).
    3. Update `ResumeBuilder.tsx`:
       - Replace the fragmented inline sections in the `Editor` constant with the new `ResumeEditor` component.
       - Pass `activeData` to `ResumeEditor`.
       - Pass a sync handler that calls `setOriginal(data)` if `!isPreviewingDraft`.
       - Ensure `disabled={isPreviewingDraft}` is passed down to all forms.
    4. Run Biome check and fix formatting.
  </action>
  <verify>
    <automated>npm run check</automated>
  </verify>
  <done>Unified editor integrated and synced with Zustand store.</done>
</task>

</tasks>

<verification>
- Verify that typing in the editor updates the RHS preview after a short delay.
- Verify that adding an Experience item immediately shows a new entry in the preview.
- Verify that "Agentic Fabricate" still works and populates the form correctly (via `reset` in `ResumeEditor`).
</verification>

<success_criteria>
- Single `FormProvider` manages the entire resume state.
- Debounced sync prevents UI lag during typing.
- All resume sections (including skills/projects) are editable.
- Full parity between store data and form state.
</success_criteria>

<output>
After completion, create `.planning/phases/06-pdf-export/06-01-SUMMARY.md`
</output>
