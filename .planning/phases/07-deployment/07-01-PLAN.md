---
phase: 07-deployment
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [
  "next.config.js",
  "src/app/api/workflow/fabricate/route.ts",
  "src/app/actions/condense.ts",
  "src/app/actions/tailor.ts",
  "src/app/actions/extract.ts",
  "src/app/actions/refine.ts",
  "src/env.js"
]
autonomous: true
requirements: [DEPLOY]

must_haves:
  truths:
    - "Next.js is configured to handle Mastra core packages as external"
    - "AI-heavy routes have extended timeouts (60s) for serverless execution"
    - "Environment variables are strictly validated for production"
---

<objective>
Configure Vercel-ready settings to prepare the application for serverless environment constraints.

Purpose: Prevent 504 Gateway Timeouts during long AI workflows and ensure Mastra correctly initializes in production.
Output: A Vercel-optimized configuration and timeout-resistant API routes.
</objective>

<execution_context>
@C:/Users/kshir/.gemini/get-shit-done/workflows/execute-plan.md
@C:/Users/kshir/.gemini/get-shit-done/templates/summary.md
</execution_context>

<tasks>

<task type="auto">
  <name>Task 1: Configure Next.js for Mastra</name>
  <files>next.config.js</files>
  <action>
    Update `next.config.js` to include `serverExternalPackages: ["@mastra/core", "@mastra/libsql"]`. This ensures the binary components of LibSQL and the core logic of Mastra are handled correctly by the Vercel build process.
  </action>
  <done>next.config.js updated with serverExternalPackages.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Serverless Timeouts</name>
  <files>
    src/app/api/workflow/fabricate/route.ts,
    src/app/actions/condense.ts,
    src/app/actions/tailor.ts,
    src/app/actions/extract.ts,
    src/app/actions/refine.ts
  </files>
  <action>
    Add `export const maxDuration = 60;` to all 5 files. This overrides the default Vercel function timeout (10-15s) to allow enough time for multi-step agentic workflows and complex PDF parsing.
  </action>
  <done>All AI entry points have maxDuration set to 60.</done>
</task>

<task type="auto">
  <name>Task 3: Production Env Refinement</name>
  <files>src/env.js</files>
  <action>
    Review `src/env.js` to ensure `GOOGLE_GENERATIVE_AI_API_KEY` is not optional in non-development environments.
  </action>
  <done>Env validation strictly enforces required keys for production.</done>
</task>

</tasks>

<verification>
Run `npm run build` locally to verify that the configurations are valid and the build process succeeds with the new settings.
</verification>

<output>
After completion, create `.planning/phases/07-deployment/07-01-SUMMARY.md`
</output>
