---
phase: 05-visual-orchestration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/schemas/resume.ts, src/mastra/agents/index.ts, src/mastra/workflows/fabricator.ts]
autonomous: true
requirements: [DESIGN-SCHEMA, STYLIST-AGENT]

must_haves:
  truths:
    - "Resume schema includes layout and theme settings"
    - "AI can automatically select a layout (classic, sidebar) based on career density"
    - "The Mastra workflow includes a final 'Stylist' step to configure visuals"
  artifacts:
    - path: "src/schemas/resume.ts"
      provides: "DesignSchema for layout/theme"
      contains: "z.enum(['classic', 'modern', 'sidebar'])"
    - path: "src/mastra/agents/index.ts"
      provides: "Stylist Agent"
      contains: "id: "stylist-agent""
    - path: "src/mastra/workflows/fabricator.ts"
      provides: "Four-step fabrication workflow"
      contains: "then(stylistStep)"
  key_links:
    - from: "src/mastra/workflows/fabricator.ts"
      to: "src/schemas/resume.ts"
      via: "zod validation of design object"
    - from: "src/mastra/agents/index.ts"
      to: "gemini-3-flash-preview"
      via: "Mastra Agent model configuration"
---

<objective>
Implement the "Stylist" foundation by extending the Zod schema with design settings and adding a dedicated Mastra agent/step to the fabrication workflow.

Purpose: To allow AI to make document design decisions (layout, theme, spacing) based on content density and career stage.
Output: A resume schema that supports visual orchestration and an AI workflow that populates these settings.
</objective>

<execution_context>
@C:/Users/kshir/.gemini/get-shit-done/workflows/execute-plan.md
@C:/Users/kshir/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-visual-orchestration/05-RESEARCH.md
@src/schemas/resume.ts
@src/mastra/agents/index.ts
@src/mastra/workflows/fabricator.ts

<interfaces>
From src/schemas/resume.ts:
```typescript
export const ResumeSchema = z.object({
	personalInfo: PersonalInformationSchema,
	experience: z.array(ExperienceSchema).default([]),
	education: z.array(EducationSchema).default([]),
	skills: z.array(SkillSchema).default([]),
	projects: z.array(ProjectSchema).default([]),
	customSections: z.array(CustomSectionSchema).default([]),
});
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend Zod Schema with Design Settings</name>
  <files>src/schemas/resume.ts</files>
  <behavior>
    - Ensure DesignSchema contains template, theme (primaryColor, fontSize, lineHeight), and layout (mainSections, sidebarSections).
    - Ensure ResumeSchema includes the design field with sensible defaults.
    - Test: DEFAULT_RESUME should include the new design object.
  </behavior>
  <action>
    Add DesignSchema to src/schemas/resume.ts as researched.
    - templates: 'classic', 'modern', 'sidebar'
    - fontSize: 'small', 'medium', 'large'
    - lineHeight: 'tight', 'relaxed'
    Update ResumeSchema to include `design: DesignSchema`.
    Update DEFAULT_RESUME and ResumeData type.
  </action>
  <verify>
    <automated>npx ts-node -e "import { DEFAULT_RESUME } from './src/schemas/resume'; console.log(JSON.stringify(DEFAULT_RESUME.design, null, 2))"</automated>
  </verify>
  <done>Schema updated and validated with new design fields.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Stylist Agent</name>
  <files>src/mastra/agents/index.ts, src/mastra/index.ts</files>
  <action>
    - Create `stylistAgent` in src/mastra/agents/index.ts using model `google/gemini-3-flash-preview` (per prompt).
    - Provide instructions for analyzing "Career Density":
      - Low Density (<400 words) -> 'classic' with 'relaxed' spacing.
      - High Density (>600 words) -> 'sidebar' with 'tight' spacing.
      - Section Mapping: Move 'skills', 'education', and specific custom sections to sidebar if using sidebar layout.
    - Register agent in src/mastra/index.ts.
  </action>
  <verify>
    <automated>grep "stylist-agent" src/mastra/agents/index.ts src/mastra/index.ts</automated>
  </verify>
  <done>Stylist Agent implemented and registered.</done>
</task>

<task type="auto">
  <name>Task 3: Add Stylist Step to Fabrication Workflow</name>
  <files>src/mastra/workflows/fabricator.ts</files>
  <action>
    - Define `stylistStep` in src/mastra/workflows/fabricator.ts.
    - It should take the output of `fabricate-resume`.
    - It calls `stylistAgent` to generate the `DesignSchema` object.
    - It merges the design object into the resume data.
    - Update `fabricatorWorkflow` to add `.then(stylistStep)` after `fabricate-resume`.
    - Ensure `outputSchema` of workflow includes the design field (which it will if ResumeSchema is updated).
  </action>
  <verify>
    <automated>grep "stylistStep" src/mastra/workflows/fabricator.ts</automated>
  </verify>
  <done>Fabrication workflow updated with a fourth step for visual orchestration.</done>
</task>

</tasks>

<verification>
- Run workflow manually or check type safety.
- Verify ResumeSchema.parse handles design field.
</verification>

<success_criteria>
- Zod schema reflects visual orchestration requirements.
- Mastra workflow produces a resume with a valid design blueprint.
- No regression in content fabrication logic.
</success_criteria>

<output>
After completion, create `.planning/phases/05-visual-orchestration/05-01-SUMMARY.md`
</output>
