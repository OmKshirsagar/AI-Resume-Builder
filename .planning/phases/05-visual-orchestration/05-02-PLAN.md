---
phase: 05-visual-orchestration
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified: [src/components/preview/PreviewPane.tsx, src/components/preview/A4Page.tsx, src/components/ResumeBuilder.tsx]
autonomous: true
requirements: [MULTI-LAYOUT, SMART-INLINE]

must_haves:
  truths:
    - "Preview engine dynamically switches layout based on AI-generated design settings"
    - "High-density lists automatically switch to inline rendering to save space"
    - "The resume maintains strictly 1-page A4 dimensions regardless of layout"
  artifacts:
    - path: "src/components/preview/PreviewPane.tsx"
      provides: "Dynamic grid-based preview engine"
    - path: "src/components/ResumeBuilder.tsx"
      provides: "Updated draft handling for design settings"
---

<objective>
Implement the dynamic Multi-Layout preview engine and "Smart Inline" list rendering to realize the visual orchestration designed by the AI agents.

Purpose: To visually apply the "Design Blueprint" generated by the Stylist Agent.
Output: A flexible, high-density preview UI that supports multiple layouts and space-saving list formats.
</objective>

<execution_context>
@C:/Users/kshir/.gemini/get-shit-done/workflows/execute-plan.md
@C:/Users/kshir/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-visual-orchestration/05-RESEARCH.md
@src/schemas/resume.ts
@src/components/preview/PreviewPane.tsx
@src/components/preview/A4Page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build Multi-Layout Grid System</name>
  <files>src/components/preview/PreviewPane.tsx, src/components/preview/A4Page.tsx</files>
  <action>
    - Refactor `PreviewPane.tsx` to read `activeData.design`.
    - Implement conditional grid layout:
      - 'classic': Single column (max-w-prose centered).
      - 'sidebar': `grid grid-cols-[2fr_1fr]` or `grid-cols-[1fr_2fr]` based on `sidebarSections`.
    - Ensure `A4Page` container handles the overflow properly with the new grid.
    - Map sections to their respective columns (main vs sidebar) based on `design.layout`.
  </action>
  <done>Preview engine supports dynamic layout switching.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Smart Inline List Rendering</name>
  <files>src/components/preview/PreviewPane.tsx</files>
  <action>
    - Create a helper component or logic to detect "Dense Lists" (e.g., Skills, Languages).
    - If a section meets the "Smart Inline" heuristic (e.g., many short items), render as a bulleted horizontal line (Item • Item • Item) instead of a vertical list.
    - Apply this to Skills, Languages, and Awards sections.
  </action>
  <done>List-based sections use space-optimized inline rendering where appropriate.</done>
</task>

<task type="auto">
  <name>Task 3: Update ResumeBuilder for Design Preview</name>
  <files>src/components/ResumeBuilder.tsx</files>
  <action>
    - Ensure `draft` preview correctly applies the AI-generated `design` settings.
    - Add visual feedback when the Stylist Agent completes its work (e.g., status message "Styling document...").
    - Ensure "Apply" correctly merges both content AND design settings into the `original` store.
  </action>
  <done>UI correctly handles and applies AI-generated design choices.</done>
</task>

</tasks>

<verification>
- Trigger "Agentic Fabricate" and verify that the layout changes (e.g., to sidebar) if the content is dense.
- Verify that short lists are rendered inline to save space.
- Ensure the preview still scales correctly to fit the RHS panel.
</verification>

<success_criteria>
- AI can successfully change the resume layout between single and two-column.
- Custom sections and standard sections are intelligently placed in columns.
- Space efficiency is significantly improved through inline rendering.
</success_criteria>

<output>
After completion, create `.planning/phases/05-visual-orchestration/05-02-SUMMARY.md`
</output>
