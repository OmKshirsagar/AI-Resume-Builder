---
phase: 10-dashboard-import
plan: 02
type: execute
wave: 2
depends_on: [10-01]
files_modified: [src/app/editor/[id]/page.tsx, src/components/editor/LinkedInImportModal.tsx, src/components/ResumeBuilder.tsx]
autonomous: true
requirements: [RESUME-MANAGEMENT, PDF-EXTRACTION]
---

<objective>
Implement the LinkedIn guided import flow and enable dynamic routing for editing specific resumes.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Build LinkedIn Import Modal</name>
  <files>src/components/editor/LinkedInImportModal.tsx</files>
  <action>
    - Create a multi-step modal:
      - Step 1: Visual guide on how to use LinkedIn's "Save to PDF" feature.
      - Step 2: Upload dropzone (re-using part of PDFUpload logic).
      - Step 3: Success state with direct link to the new Master resume.
    - Ensure newly imported resumes are saved with `isMaster: true`.
  </action>
  <verify>Open modal, follow steps, and confirm a new "Master" resume is created in the DB.</verify>
  <done>LinkedIn onboarding is easy and guided.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Dynamic Editor Routing</name>
  <files>src/app/editor/[id]/page.tsx, src/components/ResumeBuilder.tsx</files>
  <action>
    - Create dynamic route `/editor/[id]` to load specific resumes.
    - Refactor `ResumeBuilder.tsx` to fetch the resume by ID on load instead of relying solely on LocalStorage.
    - Update `useResumeStore` to handle initialization from a fetched record.
  </action>
  <verify>Clicking "Edit" on a resume in the dashboard opens the correct document in the editor.</verify>
  <done>System supports persistent document-specific editing.</done>
</task>

</tasks>

<success_criteria>
- Users can import their LinkedIn data via PDF with clear visual guidance.
- Specific resumes can be loaded and edited via unique URLs (/editor/[id]).
- Newly created/imported resumes are correctly tagged as "Master".
</success_criteria>

<output>
After completion, create `.planning/phases/10-dashboard-import/10-02-SUMMARY.md`
</output>
