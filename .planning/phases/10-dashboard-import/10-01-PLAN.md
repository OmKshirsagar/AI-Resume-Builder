---
phase: 10-dashboard-import
plan: 01
type: execute
wave: 1
depends_on: [08-auth-db, 09-local-parser]
files_modified: [src/app/dashboard/page.tsx, src/app/actions/dashboard.ts, src/components/dashboard/ResumeCard.tsx, src/components/dashboard/ResumeList.tsx]
autonomous: true
requirements: [RESUME-MANAGEMENT]
---

<objective>
Build the User Dashboard foundation and implement core CRUD actions for resume management.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Implement Dashboard Server Actions</name>
  <files>src/app/actions/dashboard.ts</files>
  <action>
    - Implement `listResumes()`: Fetch all resumes for current user, calculating `tailoringCount` via subquery on `parentId`.
    - Implement `deleteResume(id: string)`: standard Drizzle delete (cascades automatically).
    - Implement `renameResume(id: string, newTitle: string)`: update title field.
  </action>
  <verify>Manually test actions with a script or via temporary UI triggers.</verify>
  <done>Backend support for dashboard is ready.</done>
</task>

<task type="auto">
  <name>Task 2: Create Dashboard Page and Layout</name>
  <files>src/app/dashboard/page.tsx</files>
  <action>
    - Create a modern, high-fidelity dashboard layout.
    - Include a header with "Create New" action and User Button.
    - Implement "Empty State" for new users.
    - Fetch and display the list of resumes using the new server actions.
  </action>
  <verify>Navigate to /dashboard and see current resumes listed.</verify>
  <done>Users have a central home for their documents.</done>
</task>

<task type="auto">
  <name>Task 3: Build Resume List Components</name>
  <files>src/components/dashboard/ResumeCard.tsx, src/components/dashboard/ResumeList.tsx</files>
  <action>
    - Create `ResumeList` with Grid/List view toggle.
    - Create `ResumeCard` showing:
      - Title
      - Last updated date
      - "Master" badge if `isMaster` is true.
      - Tailoring count (e.g., "3 variants")
      - Actions menu (Rename, Delete, Edit).
  </action>
  <verify>UI correctly displays metadata and handles view toggles without layout shift.</verify>
  <done>Management UI is intuitive and feature-complete.</done>
</task>

</tasks>

<success_criteria>
- Users can view all their resumes in a central dashboard.
- Metadata (Last updated, Master status, tailoring count) is clearly visible.
- CRUD actions (Rename, Delete) work as expected with database persistence.
</success_criteria>

<output>
After completion, create `.planning/phases/10-dashboard-import/10-01-SUMMARY.md`
</output>
